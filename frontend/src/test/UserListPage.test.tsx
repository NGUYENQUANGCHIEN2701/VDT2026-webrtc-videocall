import { describe, it, expect } from 'vitest'

describe('UserListPage', () => {
  it('placeholder — UserListPage component exists', () => {
    // TODO: UI-02 — replaced in Plan 03 with real tests
    expect(true).toBe(true)
  })

  it.todo('UI-02: renders online users')
  it.todo('UI-02: filters self from list')
  it.todo('UI-02: shows skeleton while loading')
  it.todo('UI-02: shows empty state when no other users')
})
