---
phase: 04-1-1-call-core
plan: 05
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - frontend/src/pages/UserListPage.tsx
  - frontend/src/test/UserListPage.test.tsx
autonomous: false
requirements:
  - CALL-01
  - CALL-08
  - UI-03

must_haves:
  truths:
    - "Clicking the Call button next to a user calls startCall(username) from useCall() (CALL-01)"
    - "When callStatus === 'calling' && peerUsername === user, the Call button for that user becomes a disabled 'Calling...' button with Loader2 spinner (UI-SPEC §5.2)"
    - "When callStatus !== 'idle', all OTHER Call buttons are disabled (one outgoing call at a time)"
    - "Existing UserListPage tests still pass (no regression)"
    - "End-to-end manual verification: two browser tabs can complete a full WebRTC call from button click to video"
  artifacts:
    - path: "frontend/src/pages/UserListPage.tsx"
      provides: "UserRow wired to startCall with calling/idle state-aware buttons"
      contains: "startCall"
    - path: "frontend/src/test/UserListPage.test.tsx"
      provides: "Test for CALL-01 wire (button click → startCall) and disabled-when-calling state"
  key_links:
    - from: "frontend/src/pages/UserListPage.tsx UserRow Call button onClick"
      to: "@/contexts/CallContext startCall"
      via: "useCall() at page level, passed down as onCall prop"
      pattern: "onCall\\(user\\)"
---

<objective>
Wire the existing `UserListPage` Call button (TODO stub from Phase 3) to `startCall(username)` from `useCall()`. Implement the outgoing-call state UI per UI-SPEC §5.2: while a call is in flight, the targeted user's button becomes a disabled spinner labeled "Calling...", and all other Call buttons are disabled. End-to-end the phase: button click → CallContext starts call → callee modal appears → accept → both navigate to /call → video flows.

This plan also closes the user-visible portion of CALL-08 (cancel before connection): clicking the hang-up button on `/call` during the `calling` state sends `call-end` and returns the caller to the user list. Manual UAT in Task 2 confirms the full end-to-end vertical slice.

Purpose: Connects the caller-side trigger to the rest of the system. Without this wiring, the implemented CallContext (Plan 02) is unreachable from the UI.
Output: Complete vertical slice — Phase 4 success criteria 1-6 from ROADMAP.md (lines 102-108) all observable end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-1-1-call-core/04-CONTEXT.md
@.planning/phases/04-1-1-call-core/04-RESEARCH.md
@.planning/phases/04-1-1-call-core/04-PATTERNS.md
@.planning/phases/04-1-1-call-core/04-UI-SPEC.md
@.planning/phases/04-1-1-call-core/04-02-SUMMARY.md
@.planning/phases/04-1-1-call-core/04-03-SUMMARY.md
@.planning/phases/04-1-1-call-core/04-04-SUMMARY.md

@frontend/src/pages/UserListPage.tsx
@frontend/src/test/UserListPage.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire startCall to UserRow Call button + outgoing-call state UI (CALL-01, UI-SPEC §5.2)</name>
  <files>frontend/src/pages/UserListPage.tsx, frontend/src/test/UserListPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/UserListPage.tsx (current — UserRow at lines 28-53 has TODO stub at line 46)
    - frontend/src/test/UserListPage.test.tsx (current — existing mock setup at lines 32-51)
    - .planning/phases/04-1-1-call-core/04-PATTERNS.md (lines 518-623 — UserRow wiring target shape, COPY additions, props signature)
    - .planning/phases/04-1-1-call-core/04-UI-SPEC.md (§5.2 outgoing call state — Loader2 spinner, disabled bg-slate-700, text-emerald-400, all others disabled with opacity-50 pointer-events-none)
    - .planning/phases/04-1-1-call-core/04-CONTEXT.md (Integration Points — wire TODO stub)
  </read_first>
  <behavior>
    - Test 1 (CALL-01): with mocked `mockOnlineUsers = ['bob']`, `mockUsername = 'alice'`, `mockCallStatus = 'idle'`, clicking the button `name: 'Call bob'` calls `mockStartCall` with argument `'bob'`.
    - Test 2 (UI-SPEC §5.2 calling state): with `mockCallStatus = 'calling'`, `mockPeerUsername = 'bob'`, the button for bob is rendered with `disabled` attribute, contains text `'Calling...'`, and shows a `Loader2` spinner (queried by `aria-hidden="true"` icon or by class `animate-spin`).
    - Test 3 (UI-SPEC §5.2 other buttons disabled): with multiple online users `['bob', 'carol']`, `mockCallStatus = 'calling'`, `mockPeerUsername = 'bob'`, the button for carol is `disabled` (so the user cannot start a second concurrent call).
    - Test 4 (no regression): all 6 existing tests (renders, filters self, skeleton, empty, aria-label, logout) still pass.
  </behavior>
  <action>
    Modify `frontend/src/pages/UserListPage.tsx` per PATTERNS.md lines 542-622:

    1. Add `Loader2` to the lucide imports: `import { Video, Phone, LogOut, Users, Loader2 } from 'lucide-react'`.
    2. Add `import { useCall } from '@/contexts/CallContext'`.
    3. Extend the `COPY` object: add `callingButton: 'Calling...'` and `callingAriaLabel: (u: string) => `Calling ${u}...``.
    4. Modify `UserRow` to accept new props: `{ user, callStatus, peerUsername, onCall }: { user: string; callStatus: string; peerUsername: string | null; onCall: (username: string) => void }`. Inside UserRow compute `const isCallActive = callStatus !== 'idle'` and `const isCallingThisUser = callStatus === 'calling' && peerUsername === user`. Render the existing `<li>` with avatar + name + online badge unchanged, then conditionally render the action button:
       - If `isCallingThisUser`: a disabled Button with classes `bg-slate-700 text-emerald-400 cursor-not-allowed h-8` (matching UI-SPEC §5.2 styling), `aria-label={COPY.callingAriaLabel(user)}`, containing `<Loader2 className="animate-spin size-4 mr-2" />` and `{COPY.callingButton}`.
       - Otherwise: the existing emerald Call button extended with `disabled={isCallActive}` and `onClick={() => onCall(user)}` (replaces the TODO stub at line 46). Add the disabled style `disabled:opacity-50 disabled:pointer-events-none` to the existing className.
    5. Modify the `UserListPage` component body: add `const { callStatus, peerUsername, startCall } = useCall()` alongside the existing `useAuth()` and `useWebSocket()` calls. Pass these as props in the `.map`: `<UserRow key={user} user={user} callStatus={callStatus} peerUsername={peerUsername} onCall={startCall} />`.

    Modify `frontend/src/test/UserListPage.test.tsx`:
    1. Add a mock for `@/contexts/CallContext` mirroring the existing context mocks. Declare mutable `mockCallStatus: string = 'idle'`, `mockPeerUsername: string | null = null`, `mockStartCall = vi.fn()`, plus stubs for `acceptCall`, `rejectCall`, `hangUp` (`vi.fn()`), `localStream: null`, `remoteStream: null`, `toasts: []`.
    2. Reset all CallContext mock values to defaults in `afterEach`.
    3. Add three new tests at the end of the `describe('UserListPage')` block:
       - `it('CALL-01: clicking Call button invokes startCall with the target username')` — render with `mockOnlineUsers = ['bob']`, `userEvent.click(screen.getByRole('button', { name: 'Call bob' }))`, assert `mockStartCall.toHaveBeenCalledWith('bob')`.
       - `it('UI-SPEC §5.2: shows "Calling..." disabled button on target user during outgoing call')` — set `mockCallStatus = 'calling'`, `mockPeerUsername = 'bob'`, assert the button bearing accessible name matching `/Calling bob.../i` exists and is `disabled`.
       - `it('UI-SPEC §5.2: disables Call buttons for other users while a call is in flight')` — `mockOnlineUsers = ['bob', 'carol']`, `mockCallStatus = 'calling'`, `mockPeerUsername = 'bob'`, assert `screen.getByRole('button', { name: 'Call carol' })` has `disabled` attribute.

    Do NOT remove or modify the six existing tests. The existing tests render the component with `mockCallStatus = 'idle'` (default) so the Call button behavior they observe is unchanged.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/test/UserListPage.test.tsx --reporter=verbose && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/UserListPage.tsx` imports `Loader2` from `lucide-react`
    - `frontend/src/pages/UserListPage.tsx` imports `useCall` from `@/contexts/CallContext`
    - `frontend/src/pages/UserListPage.tsx` UserRow no longer contains the literal comment `/* TODO Phase 4 — call initiation */` (TODO stub removed)
    - `frontend/src/pages/UserListPage.tsx` UserRow renders Loader2 spinner conditional on `isCallingThisUser`
    - `frontend/src/pages/UserListPage.tsx` UserRow Call button has `disabled={isCallActive}` (or equivalent) when not in calling-this-user branch
    - `frontend/src/pages/UserListPage.tsx` COPY object contains `callingButton: 'Calling...'`
    - `frontend/src/test/UserListPage.test.tsx` contains `vi.mock('@/contexts/CallContext'`
    - `frontend/src/test/UserListPage.test.tsx` contains a test bearing the literal string `'CALL-01'` that asserts `mockStartCall` is called
    - All 6 existing UserListPage tests still pass (no regression)
    - 3 new tests pass
    - Running `npm test -- --run` from `frontend/` exits 0 with the total test count strictly greater than before this plan
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>UserListPage Call button wired to startCall; outgoing-call state UI matches §5.2; existing Phase 3 tests still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end manual UAT — full Phase 4 vertical slice across two browser tabs</name>
  <files>N/A — manual two-tab browser verification; no files modified</files>
  <action>See &lt;how-to-verify&gt; below — execute the 8 manual test cases in two browser tabs against running backend + frontend dev server.</action>
  <verify>
    <automated>MISSING — manual UAT only; jsdom cannot execute real WebRTC (per VALIDATION.md §Manual-Only Verifications). Automated unit coverage is provided by Plans 02/03/04 + Plan 05 Task 1.</automated>
    <human-check>Complete all 8 manual tests in &lt;how-to-verify&gt; and report results.</human-check>
  </verify>
  <done>All 8 manual tests pass and user types "approved" with any observations.</done>
  <what-built>
    Phase 4 implementation complete. Two browser tabs running the frontend (`http://localhost:5173` after `npm run dev` in `frontend/`) with a running backend (`mvn spring-boot:run` in `backend/`) and PostgreSQL (`docker compose up postgres` if available, otherwise running locally) should be able to complete a full 1-1 video call end-to-end. All six ROADMAP success criteria for Phase 4 (lines 102-108) must be observable.
  </what-built>
  <how-to-verify>
    Prerequisites:
    1. Backend running: from `backend/`, run `mvn spring-boot:run`. Confirm `[STARTED]` log line; backend listens on `:8080`.
    2. PostgreSQL reachable on port 5432 with the project's database (Phase 1 setup).
    3. Frontend dev server: from `frontend/`, run `npm run dev`. Confirm Vite serves `http://localhost:5173`.

    Test 1 — register two users (if not already registered):
    1. Open `http://localhost:5173` in Tab A (e.g., Chrome). Register or log in as user `alice` with any password.
    2. Open `http://localhost:5173` in Tab B (e.g., Chrome Incognito, or a different browser like Firefox). Register or log in as user `bob`.
    3. In Tab A, confirm `bob` appears in the online user list. In Tab B, confirm `alice` appears.

    Test 2 — CALL-01 + CALL-02 + CALL-03 (Accept path):
    1. In Tab A (alice), click the "Call" button next to bob.
    2. Confirm the button changes to a disabled "Calling..." spinner.
    3. In Tab B (bob), confirm the IncomingCallModal appears within ~2 seconds, displays "alice" as the caller, plays the ringtone (audible 800Hz beep, ~0.3s on, 1.7s off, repeating).
    4. In Tab B, click Accept.
    5. Confirm both tabs navigate to `/call` within a few seconds.
    6. Confirm both tabs show the remote peer's video (full-screen) and local video (small mirrored PiP, bottom-right). Both users should hear each other (CALL-04).
    7. Browser tab indicator: confirm camera/mic indicator is lit on both tabs.

    Test 3 — Phase 4 success criterion 6 (stable simultaneous video):
    1. Leave the call running for 30 seconds. Confirm video remains stable, no disconnects, no console errors.

    Test 4 — CALL-08 (hang-up from /call):
    1. In Tab A, click the red End Call button (bottom center).
    2. Confirm both tabs return to the user list (`/users` route).
    3. Confirm "Call ended" toast appears briefly on Tab B (top-right per UI-SPEC §5.4).
    4. Confirm camera/mic indicator clears on both tabs (Pitfall 5 verified).

    Test 5 — CALL-03 (Reject path):
    1. From the user list in Tab A, click Call next to bob.
    2. In Tab B, click Reject in the modal.
    3. Confirm Tab A shows "Call declined" toast (red-400 text per UI-SPEC §5.4).
    4. Confirm Tab B modal closes and both return to user list with `callStatus === 'idle'`.

    Test 6 — CALL-07 (30s no-answer timeout):
    1. From Tab A, click Call next to bob.
    2. In Tab B, do NOT click Accept or Reject. Wait 30 seconds.
    3. Confirm Tab A shows "No answer" toast (amber-400) after exactly ~30s.
    4. Confirm Tab B's modal dismisses automatically (because Tab A sent `call-end`).
    5. Confirm both tabs return to user list.

    Test 7 — CALL-08 (cancel from caller before answer):
    1. From Tab A, click Call next to bob.
    2. While the modal is showing on Tab B (before bob clicks anything), navigate Tab A's URL bar to `/users` (or in a future Phase 5 iteration, click cancel — Phase 4 doesn't have a caller-side cancel button explicitly visible, but the user can hang up from `/call` once they get there, or the `call-end` is sent indirectly via navigation away). For Phase 4 verification, this test confirms that Tab A's hang-up button on `/call` (Task 4 above) cleanly cancels and Tab B's modal dismisses.
    3. Alternatively: in Tab A, while "Calling..." is showing, click any other user (if any). Confirm clicking is disabled — `disabled:pointer-events-none` per UI-SPEC §5.2.

    Test 8 — Accessibility spot-checks:
    1. With the IncomingCallModal showing, press Escape — confirm modal dismisses (rejectCall fired).
    2. Tab through the modal — confirm Accept button receives focus.

    Console verification:
    - Open browser dev tools console on both tabs.
    - Confirm NO uncaught errors during any of the above tests.
    - Acceptable: STOMP "Disconnect" log lines on hang-up; "ICE state change" logs.
    - Unacceptable: `InvalidStateError`, `Uncaught (in promise)`, `Failed to execute 'addIceCandidate'`, React rendering errors.

    Note any failures, console errors, or deviations from expected behavior. If everything passes, confirm by typing `approved` along with any observations (e.g., "ringtone slightly delayed, otherwise all 8 tests pass").
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 manual tests pass, or describe any failures/regressions/console errors observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `user` parameter passed to onCall | Comes from `onlineUsers` array, which is server-broadcast presence data (Phase 2 trust boundary). |
| Two-tab UAT environment | Both tabs run on `localhost` — same-origin. LAN demo scope per project constraints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05-01 | Spoofing | `startCall(self)` via direct DOM click on disabled-state button | mitigate | UserListPage already filters self from `otherUsers`; `CallContext.startCall` has additional guard (T-4-03 from Plan 02). |
| T-4-05-02 | Denial of Service | User clicks Call repeatedly while one is in flight | mitigate | All Call buttons disabled via `isCallActive` when `callStatus !== 'idle'`; verified by Test 3. |
| T-4-05-03 | Information Disclosure | Camera/mic indicator persists after hang-up | mitigate | Plan 02 teardown stops tracks before close(); manual Test 4 verifies indicator clears (Pitfall 5). |
| T-4-SC | Tampering | npm installs | mitigate | No new packages installed in this plan. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` exits 0 (all tests across all 5 plans green)
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run build` exits 0
- Manual UAT checkpoint: all 8 tests pass (Task 2)
</verification>

<success_criteria>
- ROADMAP §Phase 4 success criterion 1 (Call → modal + Accept/Reject + ringtone): verified by Test 2 + Test 5
- ROADMAP §Phase 4 success criterion 2 (Accept → P2P video + audio in seconds): verified by Test 2
- ROADMAP §Phase 4 success criterion 3 (Reject → modal dismisses + "Call declined" on caller): verified by Test 5
- ROADMAP §Phase 4 success criterion 4 (30s no-answer cancels + notifications): verified by Test 6
- ROADMAP §Phase 4 success criterion 5 (Either party can cancel before connection): verified by Test 7
- ROADMAP §Phase 4 success criterion 6 (Stable simultaneous local + remote video): verified by Test 3
- Phase 4 requirements CALL-01, CALL-02, CALL-03, CALL-04, CALL-07, CALL-08, UI-03 all observably complete
</success_criteria>

<output>
Create `.planning/phases/04-1-1-call-core/04-05-SUMMARY.md` when done — record UAT pass/fail per test, manual ringtone validation, any console warnings, and outstanding items to defer to Phase 5.
</output>
