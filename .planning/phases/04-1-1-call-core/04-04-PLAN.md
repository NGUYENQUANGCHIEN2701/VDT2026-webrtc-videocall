---
phase: 04-1-1-call-core
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - frontend/src/pages/CallPage.tsx
  - frontend/src/test/CallPage.test.tsx
autonomous: true
requirements:
  - UI-03
  - CALL-04
  - CALL-08

must_haves:
  truths:
    - "CallPage renders <video> with ref bound to remote stream, autoPlay, playsInline (unmuted for remote audio), aria-label='Remote video stream' (UI-SPEC §5.3)"
    - "CallPage renders local <video> as PiP overlay: ref bound to local stream, autoPlay, playsInline, muted, mirrored (scale-x-[-1]), aria-label='Local video preview' (UI-SPEC §5.3)"
    - "video.srcObject is assigned via useEffect watching the stream state (Pitfall 3 — never as JSX prop)"
    - "Peer username overlay rendered top-left when peerUsername is set (UI-SPEC §5.3)"
    - "Hang-up button rendered in bottom control bar with aria-label='End call' and triggers hangUp() (UI-03, CALL-08)"
    - "Empty-state placeholder ('Waiting for remote video...') rendered when remoteStream is null"
    - "Component unmount cleanly leaves the call alone (does not call hangUp on unmount — user may navigate away and back)"
  artifacts:
    - path: "frontend/src/pages/CallPage.tsx"
      provides: "Full CallPage UI per UI-SPEC §5.3 — remote video full-screen, local PiP, peer name overlay, hang-up button"
      contains: "srcObject"
      min_lines: 60
    - path: "frontend/src/test/CallPage.test.tsx"
      provides: "Active tests for UI-03 (video elements, hang-up button, placeholder)"
  key_links:
    - from: "frontend/src/pages/CallPage.tsx"
      to: "@/contexts/CallContext"
      via: "useCall() to read localStream, remoteStream, peerUsername, hangUp"
      pattern: "useCall"
    - from: "frontend/src/pages/CallPage.tsx local <video>"
      to: "localStream"
      via: "localVideoRef.current.srcObject = localStream inside useEffect([localStream])"
      pattern: "localVideoRef.current.srcObject"
    - from: "frontend/src/pages/CallPage.tsx remote <video>"
      to: "remoteStream"
      via: "remoteVideoRef.current.srcObject = remoteStream inside useEffect([remoteStream])"
      pattern: "remoteVideoRef.current.srcObject"
---

<objective>
Implement the full `/call` page UI per UI-SPEC §5.3. Delivers the vertical slice: `connected` state in CallContext → `navigate('/call')` lands here → remote video fills screen → local video PiP overlay → hang-up button ends the call. After this plan, two browser tabs that complete signaling can see each other's video and end the call from this page.

Purpose: User-visible call screen. Without this plan, accepting a call navigates to a route with no UI — the WebRTC tracks are flowing but invisible.
Output: Stable video call screen (UI-03) with local + remote streams, peer name overlay, and basic hang-up. Mic/camera toggles, duration timer, and connection status are explicitly out of scope (Phase 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-1-1-call-core/04-CONTEXT.md
@.planning/phases/04-1-1-call-core/04-RESEARCH.md
@.planning/phases/04-1-1-call-core/04-PATTERNS.md
@.planning/phases/04-1-1-call-core/04-UI-SPEC.md
@.planning/phases/04-1-1-call-core/04-02-SUMMARY.md

@frontend/src/pages/CallPage.tsx
@frontend/src/pages/UserListPage.tsx
@frontend/src/test/CallPage.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CallPage full UI + video srcObject wiring (UI-03, Pitfall 3)</name>
  <files>frontend/src/pages/CallPage.tsx, frontend/src/test/CallPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/CallPage.tsx (stub from Plan 01)
    - frontend/src/test/CallPage.test.tsx (scaffolds from Plan 01)
    - frontend/src/pages/UserListPage.tsx (analog for page-level structure, default export pattern, COPY object, Button usage)
    - .planning/phases/04-1-1-call-core/04-PATTERNS.md (lines 270-369 — full CallPage structure: imports, refs, useEffect for srcObject, layout JSX)
    - .planning/phases/04-1-1-call-core/04-UI-SPEC.md (§5.3 layout, §8 copy strings, §9 accessibility — remote NOT muted, local muted)
    - .planning/phases/04-1-1-call-core/04-RESEARCH.md (lines 459-473 Pitfall 3 srcObject via useEffect; lines 603-617 video wiring code example; lines 475-479 Pitfall 4 unmount handling)
  </read_first>
  <behavior>
    - Test 1 (UI-03 remote video): with mocked `remoteStream = new MediaStream()`, the rendered DOM contains a `<video>` element with `aria-label="Remote video stream"`, `autoPlay`, `playsInline`, and NOT `muted`. After mount, `remoteVideoRef.current.srcObject === remoteStream`.
    - Test 2 (UI-03 local video PiP): with mocked `localStream`, a `<video>` with `aria-label="Local video preview"`, `autoPlay`, `playsInline`, `muted`, and class containing `scale-x-[-1]` (mirrored).
    - Test 3 (UI-03 placeholder): with `remoteStream = null`, the literal text `'Waiting for remote video...'` is rendered, and the lucide `Users` icon is visible.
    - Test 4 (UI-03 peer overlay): with `peerUsername = 'bob'`, an element with text `'bob'` appears top-left (testable by class assertion `top-4 left-4` or by querying the element bearing the username text within a `bg-slate-900/70` container).
    - Test 5 (UI-03 hang-up): a button with `aria-label="End call"` exists; clicking it calls the mocked `hangUp`.
    - Test 6 (Pitfall 3): when `remoteStream` changes from null to a value, the `srcObject` of `remoteVideoRef.current` updates accordingly (testable via re-render with new mock value and asserting `.srcObject` reference equality).
  </behavior>
  <action>
    Replace the stub body of `frontend/src/pages/CallPage.tsx` with the full implementation per UI-SPEC §5.3 and PATTERNS.md lines 270-369.

    Imports: `useEffect`, `useRef` from `react`; `Button` from `@/components/ui/button`; `Users`, `PhoneOff` from `lucide-react`; `useCall` from `@/contexts/CallContext`.

    Top-of-file `COPY` per the page convention: `placeholderText: 'Waiting for remote video...'`, `endCallLabel: 'End call'`.

    Default export `function CallPage()`. Body:
    1. Destructure `{ localStream, remoteStream, peerUsername, hangUp } = useCall()`.
    2. Declare `localVideoRef = useRef<HTMLVideoElement>(null)` and `remoteVideoRef = useRef<HTMLVideoElement>(null)`.
    3. `useEffect(() => { if (localVideoRef.current) localVideoRef.current.srcObject = localStream }, [localStream])` per Pitfall 3.
    4. `useEffect(() => { if (remoteVideoRef.current) remoteVideoRef.current.srcObject = remoteStream }, [remoteStream])` per Pitfall 3.
    5. Return JSX per UI-SPEC §5.3 layout:
       - Outer container: `<div className="relative w-full h-screen bg-slate-950 overflow-hidden">`
       - Remote video: `<video ref={remoteVideoRef} className="w-full h-full object-cover" autoPlay playsInline aria-label="Remote video stream" />` (NOT muted — per UI-SPEC §9 the user must hear remote audio)
       - Empty-state placeholder shown ONLY when `!remoteStream`: `<div className="absolute inset-0 flex flex-col items-center justify-center bg-slate-950">` containing `<Users className="size-16 text-slate-700 mb-4" />` and `<p className="text-sm text-slate-500">{COPY.placeholderText}</p>`
       - Peer name overlay shown ONLY when `peerUsername`: `<div className="absolute top-4 left-4 bg-slate-900/70 backdrop-blur-sm rounded-lg px-3 py-1"><span className="text-sm font-semibold text-slate-50">{peerUsername}</span></div>`
       - Local PiP container: `<div className="absolute bottom-24 right-4 w-32 aspect-video bg-slate-800 rounded-lg overflow-hidden border border-slate-700 shadow-lg animate-in fade-in zoom-in-90 duration-200">` containing `<video ref={localVideoRef} className="w-full h-full object-cover scale-x-[-1]" autoPlay playsInline muted aria-label="Local video preview" />`
       - Control bar: `<div className="absolute bottom-0 left-0 right-0 h-20 bg-slate-900/90 backdrop-blur-sm border-t border-slate-700 flex items-center justify-center gap-4">` containing the hang-up `<Button className="h-12 w-12 rounded-full bg-red-600 hover:bg-red-700 text-white" aria-label={COPY.endCallLabel} onClick={hangUp}><PhoneOff className="size-5" /></Button>`

    Do NOT add a beforeunload listener or call `hangUp` on unmount — per RESEARCH Pitfall 4 the cleanest approach is to leave the call state intact when the user navigates away from `/call`, so they can navigate back. Phase 5 will add proper unmount handling if needed. Document this explicitly with an inline `// Phase 4: deliberately no unmount cleanup — see RESEARCH.md Pitfall 4 / Phase 5 TODO`.

    Convert `it.skip` → `it` in `frontend/src/test/CallPage.test.tsx` for all five UI-03 tests + the new Pitfall-3 srcObject test. Mock `useCall` with mutable `mockLocalStream`, `mockRemoteStream`, `mockPeerUsername`, `mockHangUp`. Use `new MediaStream()` if available in jsdom; otherwise mock as `const fakeStream = { getTracks: () => [] } as unknown as MediaStream` and assert via reference equality. For the srcObject pitfall test: render with `remoteStream = null`, then `rerender` with a fake stream, and assert `screen.queryByLabelText('Remote video stream') as HTMLVideoElement`.srcObject equals the fake stream.

    Render via `<MemoryRouter initialEntries={['/call']}><CallPage /></MemoryRouter>`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/test/CallPage.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/CallPage.tsx` has a `default export` named `CallPage`
    - `frontend/src/pages/CallPage.tsx` contains TWO `useEffect` blocks assigning `.srcObject` (one for local, one for remote)
    - `frontend/src/pages/CallPage.tsx` contains the literal `aria-label="Remote video stream"`
    - `frontend/src/pages/CallPage.tsx` contains the literal `aria-label="Local video preview"`
    - `frontend/src/pages/CallPage.tsx` local `<video>` has `muted` attribute; remote `<video>` does NOT have `muted`
    - `frontend/src/pages/CallPage.tsx` local video class list contains `scale-x-[-1]` (mirrored per UI-SPEC §5.3)
    - `frontend/src/pages/CallPage.tsx` contains the literal text `'Waiting for remote video...'` (UI-SPEC §8)
    - `frontend/src/pages/CallPage.tsx` contains `aria-label={COPY.endCallLabel}` or `aria-label="End call"` on the hang-up button (UI-SPEC §8)
    - `frontend/src/pages/CallPage.tsx` hang-up button onClick references `hangUp` from `useCall()`
    - `frontend/src/pages/CallPage.tsx` peer overlay class list contains `top-4 left-4 bg-slate-900/70`
    - `frontend/src/pages/CallPage.tsx` local PiP container class list contains `bottom-24 right-4 w-32 aspect-video`
    - `frontend/src/pages/CallPage.tsx` control bar class list contains `absolute bottom-0 left-0 right-0 h-20`
    - `frontend/src/pages/CallPage.tsx` contains the inline comment marker `Pitfall 4` (documenting deliberate unmount behavior)
    - `frontend/src/test/CallPage.test.tsx` contains no remaining `it.skip(` (all six tests active)
    - Behavior: rendering CallPage with `mockRemoteStream = null` shows the placeholder text; with a stream, the placeholder is hidden
    - Behavior: clicking the End call button invokes `mockHangUp`
    - Running `npm test -- --run` from `frontend/` exits 0 (all tests green)
    - `cd frontend && npx tsc --noEmit` exits 0 AND `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>CallPage renders the full §5.3 layout, wires both video refs via useEffect (Pitfall 3 closed), hang-up button calls CallContext.hangUp(). UI-03 verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `remoteStream` ↔ `<video>` element | Stream object comes from RTCPeerConnection `ontrack` event (browser-native, validated). |
| `peerUsername` displayed | Same trust as IncomingCallModal — server-overwritten `from` field is the source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04-01 | Information Disclosure | Remote video unmuted by default | accept | UI-SPEC §9 requires unmuted remote audio. Browser autoplay policy for video-with-audio may require user gesture; AcceptCall click satisfies this. |
| T-4-04-02 | Denial of Service | srcObject set via JSX prop (Pitfall 3) | mitigate | Both `<video>` refs use `useEffect` watching the stream state — never as JSX prop. Acceptance criterion enforces two `useEffect` blocks assigning `srcObject`. |
| T-4-04-03 | Denial of Service | Camera/mic still active after navigation away from /call | accept | Phase 4 deliberately leaves call state intact on unmount (Pitfall 4 documented inline). User can navigate back. If user logs out, `disconnect` from WebSocketContext + STOMP disconnect causes the call to be torn down indirectly. Phase 5 will refine unmount semantics. |
| T-4-SC | Tampering | npm installs | mitigate | No new packages installed. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` exits 0 with all CallPage tests green
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run build` exits 0
- Both `<video>` elements wired via `useEffect` (Pitfall 3 closed)
- Hang-up button is the ONLY interactive control (Phase 5 adds mic/camera/duration)
</verification>

<success_criteria>
- CallPage vertical slice complete: connected state → /call route → remote video full-screen + local PiP + hang-up button → hang-up clears state through CallContext
- UI-03 verified by unit tests (video element refs, aria-labels, hang-up button)
- Remote video unmuted (callee hears caller and vice versa)
- Local video muted + mirrored (per UI-SPEC §5.3 and §9)
</success_criteria>

<output>
Create `.planning/phases/04-1-1-call-core/04-04-SUMMARY.md` when done — record CallPage layout, srcObject wiring pattern verified, Pitfall 3 mitigation in place, and any deviations from UI-SPEC §5.3.
</output>
