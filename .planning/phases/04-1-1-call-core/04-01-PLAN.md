---
phase: 04-1-1-call-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/contexts/CallContext.tsx
  - frontend/src/components/IncomingCallModal.tsx
  - frontend/src/hooks/useRingtone.ts
  - frontend/src/pages/CallPage.tsx
  - frontend/src/test/setup.ts
  - frontend/src/test/CallContext.test.tsx
  - frontend/src/test/IncomingCallModal.test.tsx
  - frontend/src/test/CallPage.test.tsx
autonomous: true
requirements:
  - CALL-01
  - CALL-02
  - CALL-03
  - CALL-04
  - CALL-07
  - CALL-08
  - UI-03

must_haves:
  truths:
    - "Test infrastructure exists for CallContext, IncomingCallModal, and CallPage"
    - "RTCPeerConnection is mocked globally in jsdom"
    - "main.tsx wraps the app in BrowserRouter, AuthProvider, WebSocketProvider, and CallProvider (in that order, outermost to innermost) per D-04"
    - "App.tsx is a pure routes component with /login, /users, /call routes, plus IncomingCallModal and toast overlay rendered outside <Routes>"
    - "CallContext stub provides correct type contracts (CallContextValue interface) so downstream plans can compile against it"
    - "useCall() hook is exported and throws if used outside CallProvider"
    - "All Wave 0 test files exist with at least one passing assertion (test scaffolding pattern)"
  artifacts:
    - path: "frontend/src/main.tsx"
      provides: "Provider tree per D-04: BrowserRouter > AuthProvider > WebSocketProvider > CallProvider > App"
      contains: "BrowserRouter"
    - path: "frontend/src/App.tsx"
      provides: "Pure routes component with /call route and IncomingCallModal overlay"
      contains: "/call"
    - path: "frontend/src/contexts/CallContext.tsx"
      provides: "CallContext skeleton exporting CallProvider and useCall(); CallContextValue interface defined"
      exports: ["CallProvider", "useCall"]
    - path: "frontend/src/components/IncomingCallModal.tsx"
      provides: "Stub component returning null when callStatus !== 'ringing'"
      exports: ["IncomingCallModal"]
    - path: "frontend/src/hooks/useRingtone.ts"
      provides: "Hook stub (no-op useEffect) to satisfy import in IncomingCallModal"
      exports: ["useRingtone"]
    - path: "frontend/src/pages/CallPage.tsx"
      provides: "Stub page component (default export)"
    - path: "frontend/src/test/setup.ts"
      provides: "Global RTCPeerConnection mock via vi.stubGlobal"
      contains: "RTCPeerConnection"
    - path: "frontend/src/test/CallContext.test.tsx"
      provides: "Test scaffold for CALL-01/02/03/04/07/08"
    - path: "frontend/src/test/IncomingCallModal.test.tsx"
      provides: "Test scaffold for CALL-02 (modal UI) + CALL-03 (accept/reject)"
    - path: "frontend/src/test/CallPage.test.tsx"
      provides: "Test scaffold for UI-03 (video elements + hang-up)"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "@/contexts/CallContext"
      via: "CallProvider import + JSX wrapping"
      pattern: "CallProvider"
    - from: "frontend/src/App.tsx"
      to: "@/components/IncomingCallModal"
      via: "JSX render outside <Routes>"
      pattern: "<IncomingCallModal"
    - from: "frontend/src/App.tsx"
      to: "@/contexts/CallContext"
      via: "useCall() to read toasts"
      pattern: "useCall"
    - from: "frontend/src/test/setup.ts"
      to: "global RTCPeerConnection"
      via: "vi.stubGlobal"
      pattern: "vi.stubGlobal\\('RTCPeerConnection'"
---

<objective>
Establish Phase 4 test infrastructure (RTCPeerConnection global mock, three new test files with scaffolding) and refactor the application shell (per D-04) so that downstream plans can implement features without restructuring the provider tree. Creates stub files for CallContext, IncomingCallModal, useRingtone, and CallPage so that App.tsx and main.tsx compile against the final target shape. Tests fail loudly (acceptance criteria assert "stub" behavior) until later waves replace the stubs.

Purpose: Unblock Waves 1-3 by establishing the file structure, type contracts, provider order (D-04), and test scaffolding all at once. After this plan, downstream plans only modify isolated component bodies and never touch the routing or provider tree.
Output: Compiling frontend with stub providers/components, test suite green (existing tests + new stub tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1-1-call-core/04-CONTEXT.md
@.planning/phases/04-1-1-call-core/04-RESEARCH.md
@.planning/phases/04-1-1-call-core/04-PATTERNS.md
@.planning/phases/04-1-1-call-core/04-UI-SPEC.md
@.planning/phases/04-1-1-call-core/04-VALIDATION.md

# Existing files that this plan modifies or analogs
@frontend/src/main.tsx
@frontend/src/App.tsx
@frontend/src/contexts/WebSocketContext.tsx
@frontend/src/test/setup.ts
@frontend/src/test/UserListPage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global RTCPeerConnection mock + CallContext stub provider</name>
  <files>frontend/src/test/setup.ts, frontend/src/contexts/CallContext.tsx</files>
  <read_first>
    - frontend/src/test/setup.ts (current 7-line file)
    - frontend/src/contexts/WebSocketContext.tsx (analog for context skeleton — exact import order, createContext pattern, useXxx() hook export)
    - .planning/phases/04-1-1-call-core/04-PATTERNS.md (lines 25-138 — CallContext interface + provider skeleton; lines 709-727 — RTCPeerConnection mock pattern)
    - .planning/phases/04-1-1-call-core/04-VALIDATION.md (lines 66-83 — MockPeerConnection canonical shape)
    - .planning/phases/04-1-1-call-core/04-RESEARCH.md (lines 711-727 — jsdom RTCPeerConnection rationale)
  </read_first>
  <action>
    Extend `frontend/src/test/setup.ts`: add a top-level `vi.stubGlobal('RTCPeerConnection', MockRTCPeerConnection)` where `MockRTCPeerConnection` is a class exposing `addTrack`, `createOffer` (returns `{ type: 'offer', sdp: 'mock-sdp' }`), `createAnswer` (returns `{ type: 'answer', sdp: 'mock-sdp' }`), `setLocalDescription`, `setRemoteDescription`, `addIceCandidate`, `close` as `vi.fn()`, plus mutable instance fields `onicecandidate`, `ontrack`, `oniceconnectionstatechange` (all `null`) and `iceConnectionState` (`'new'`). Import `vi` from `vitest`. Keep the existing `afterEach(cleanup)` block. Also stub `navigator.mediaDevices.getUserMedia` to resolve with a mock `MediaStream` exposing `getTracks()` returning an array of mock tracks each with a `stop` `vi.fn()`.

    Create `frontend/src/contexts/CallContext.tsx` as a stub per D-01/D-02/D-03. Exports must include: `CallProvider` (function component accepting `{ children: ReactNode }`), `useCall` (hook reading the context, throwing `'useCall must be used inside CallProvider'` on null), and exported types `CallStatus = 'idle' | 'calling' | 'ringing' | 'connected' | 'ended'`, `Toast = { id: string; message: string; style: string }`, and `CallContextValue` interface with fields `localStream: MediaStream | null`, `remoteStream: MediaStream | null`, `callStatus: CallStatus`, `peerUsername: string | null`, `toasts: Toast[]`, `startCall: (targetUsername: string) => Promise<void>`, `acceptCall: () => Promise<void>`, `rejectCall: () => void`, `hangUp: () => void`. The stub `CallProvider` returns a value where every field/function is a no-op or constant default — `callStatus: 'idle'`, both streams null, `peerUsername: null`, `toasts: []`, and async functions resolve immediately. Do not implement WebRTC logic here (Plan 02 owns the real implementation). Import order must mirror `WebSocketContext.tsx` lines 1-2 plus the additional `useNavigate`, `useWebSocket`, `useAuth` imports the next plan will need.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/test/setup.ts` contains the literal string `vi.stubGlobal('RTCPeerConnection'`
    - `frontend/src/test/setup.ts` contains the literal string `getUserMedia`
    - `frontend/src/contexts/CallContext.tsx` exports `CallProvider`, `useCall`, and type `CallContextValue`
    - `frontend/src/contexts/CallContext.tsx` contains `createContext<CallContextValue | null>(null)`
    - `frontend/src/contexts/CallContext.tsx` contains `'useCall must be used inside CallProvider'`
    - `frontend/src/contexts/CallContext.tsx` defines callStatus union with exactly five values: idle, calling, ringing, connected, ended (per D-03)
    - Running `npm test -- --run` from `frontend/` exits 0 with all existing Phase 3 tests still passing
    - TypeScript compile check `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RTCPeerConnection is globally mockable in jsdom; CallContext skeleton exports the locked interface and useCall() hook so downstream files compile.</done>
</task>

<task type="auto">
  <name>Task 2: Stub IncomingCallModal, useRingtone, CallPage components + provider tree refactor (D-04, D-06, D-07)</name>
  <files>frontend/src/hooks/useRingtone.ts, frontend/src/components/IncomingCallModal.tsx, frontend/src/pages/CallPage.tsx, frontend/src/main.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/main.tsx (current 16-line file — must be replaced)
    - frontend/src/App.tsx (current 28-line file — must be replaced)
    - frontend/src/contexts/CallContext.tsx (just created in Task 1 — read to confirm CallContextValue shape)
    - .planning/phases/04-1-1-call-core/04-PATTERNS.md (lines 142-188 useRingtone, 192-262 IncomingCallModal, 270-369 CallPage layout, 374-454 modified App.tsx, 458-514 modified main.tsx)
    - .planning/phases/04-1-1-call-core/04-CONTEXT.md (D-04, D-06, D-07 — provider order, modal overlay placement, navigation pattern)
    - .planning/phases/04-1-1-call-core/04-UI-SPEC.md (§5.1 modal layout, §5.3 CallPage layout, §5.4 toast container)
  </read_first>
  <action>
    Create `frontend/src/hooks/useRingtone.ts` exporting `function useRingtone()`. Stub body: a `useEffect(() => { /* Plan 03 implements Web Audio API per D-10/D-11 */ }, [])` with empty cleanup. Import `useEffect` from `react`. This file must compile so `IncomingCallModal` can import it now.

    Create `frontend/src/components/IncomingCallModal.tsx` exporting named `IncomingCallModal` (no props). Body: call `useCall()` (destructure `callStatus`), call `useRingtone()`, and `if (callStatus !== 'ringing') return null`. Otherwise return a minimal `<div role="dialog" aria-modal="true" aria-labelledby="modal-caller-name">Stub modal</div>` placeholder. The real modal markup per UI-SPEC §5.1 is owned by Plan 03.

    Create `frontend/src/pages/CallPage.tsx` as default export `function CallPage()` returning a minimal `<div className="relative w-full h-screen bg-slate-950 overflow-hidden">Stub call page</div>`. The full UI per UI-SPEC §5.3 is owned by Plan 04.

    Replace `frontend/src/main.tsx` with the target shape per D-04: imports `BrowserRouter` from `react-router-dom`, `CallProvider` from `@/contexts/CallContext`, and wraps the tree in order (outermost first): `React.StrictMode > BrowserRouter > AuthProvider > WebSocketProvider > CallProvider > App`. The exact provider order must match D-04 verbatim — `BrowserRouter` outermost so `useNavigate` works inside `CallProvider`, `AuthProvider` before `WebSocketProvider` because the STOMP client needs the auth token, `CallProvider` innermost because it depends on `useWebSocket()` and `useAuth()`.

    Replace `frontend/src/App.tsx`: remove `BrowserRouter` import + JSX (moved to main.tsx); add imports for `CallPage`, `IncomingCallModal`, and `useCall`; add the `/call` route wrapped in `ProtectedRoute`; render `<IncomingCallModal />` OUTSIDE `<Routes>` (per D-06 — overlays any route); render a toast container `<div role="status" aria-live="polite" className="fixed top-4 right-4 z-50 flex flex-col gap-2">` mapping `toasts` from `useCall()` per UI-SPEC §5.4 (Plan 02 will populate the `toasts` array). The component returns a React fragment `<>...</>` wrapping `<Routes>` plus the global overlays. Keep the existing `/login` and `/users` routes and the root redirect (`<Navigate to={token ? '/users' : '/login'} replace />`).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useRingtone.ts` exports `useRingtone` (named export)
    - `frontend/src/components/IncomingCallModal.tsx` exports `IncomingCallModal` (named export) and contains `if (callStatus !== 'ringing') return null`
    - `frontend/src/pages/CallPage.tsx` has a default export named `CallPage`
    - `frontend/src/main.tsx` contains `BrowserRouter` import from `react-router-dom`
    - `frontend/src/main.tsx` contains `CallProvider` import from `@/contexts/CallContext`
    - Provider order in `main.tsx` JSX, from outermost to innermost, is exactly: `React.StrictMode`, `BrowserRouter`, `AuthProvider`, `WebSocketProvider`, `CallProvider`, `App` (D-04)
    - `frontend/src/App.tsx` does NOT import `BrowserRouter` (moved to main.tsx)
    - `frontend/src/App.tsx` contains a `<Route path="/call"` wrapped by `<ProtectedRoute>`
    - `frontend/src/App.tsx` renders `<IncomingCallModal />` outside `<Routes>` (D-06)
    - `frontend/src/App.tsx` contains a `role="status"` `aria-live="polite"` toast container
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
    - `cd frontend && npm test -- --run` exits 0 (all Phase 3 tests still pass after provider tree refactor)
  </acceptance_criteria>
  <done>App shell matches D-04/D-06/D-07 target shape; all stub component files exist with correct exports; build and tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Test scaffolds for CallContext, IncomingCallModal, CallPage</name>
  <files>frontend/src/test/CallContext.test.tsx, frontend/src/test/IncomingCallModal.test.tsx, frontend/src/test/CallPage.test.tsx</files>
  <read_first>
    - frontend/src/test/UserListPage.test.tsx (analog — mock pattern, MemoryRouter wrap, mutable mock values, beforeAll/afterEach/afterAll lifecycle)
    - frontend/src/contexts/CallContext.tsx (created Task 1 — read CallContextValue to ensure mock matches)
    - frontend/src/components/IncomingCallModal.tsx (created Task 2 — confirms stub returns null when not ringing)
    - frontend/src/pages/CallPage.tsx (created Task 2 — confirms default export)
    - .planning/phases/04-1-1-call-core/04-VALIDATION.md (per-task verification map lines 39-54 — which req IDs each test owns)
    - .planning/phases/04-1-1-call-core/04-PATTERNS.md (lines 690-707 — vi.mock CallContext pattern)
  </read_first>
  <action>
    Create `frontend/src/test/CallContext.test.tsx`. Use `vi.mock('@/contexts/WebSocketContext', ...)` to mock `useWebSocket()` returning `{ client: mockClient, subscribe: mockSubscribe, publish: mockPublish, ... }` where `mockClient` is an object `{ connected: true }`, and `vi.mock('@/contexts/AuthContext', ...)` returning `{ username: 'alice', token: 'mock-token', dispatch: vi.fn() }`. Also mock `react-router-dom` to override `useNavigate` returning `mockNavigate = vi.fn()` (mirror the partial-mock pattern from `UserListPage.test.tsx` lines 22-29). Write one scaffold test per requirement ID, each describing the assertion that Plan 02 will satisfy:
    - `it.skip('CALL-01: startCall(targetUsername) sets callStatus to "calling" and publishes call-request signal')` — empty body or `expect(true).toBe(true)` placeholder
    - `it.skip('CALL-01: startCall guards against self-call (no-op when targetUsername === authUsername)')` (per threat T-4-03)
    - `it.skip('CALL-02: call-request signal sets callStatus to "ringing" and stores peerUsername from msg.from')`
    - `it.skip('CALL-03: rejectCall publishes call-decline signal and returns state to idle')`
    - `it.skip('CALL-04: offer signal calls setRemoteDescription and drains ICE candidate buffer')`
    - `it.skip('CALL-04: ice-candidate signal before setRemoteDescription buffers candidate; drains after')`
    - `it.skip('CALL-07: 30s timeout fires call-end signal and adds "No answer" toast')`
    - `it.skip('CALL-08: hangUp publishes call-end signal and runs teardown')`
    Use `it.skip()` so the file exists and is discovered, but tests do not fail in Wave 0. Include the necessary imports and the `vi.mock` blocks so Plan 02 only needs to convert `it.skip` to `it` and fill bodies.

    Create `frontend/src/test/IncomingCallModal.test.tsx`. Mock `@/contexts/CallContext` returning mutable mock values for `callStatus`, `peerUsername`, `acceptCall`, `rejectCall` (mirror `UserListPage.test.tsx` lines 32-42). Mock `@/hooks/useRingtone` as a `vi.fn()` no-op. Render `<IncomingCallModal />` via `render(...)` (no router needed). Scaffold tests:
    - `it.skip('CALL-02: renders modal when callStatus === "ringing" with caller name')`
    - `it.skip('CALL-02: renders nothing when callStatus !== "ringing"')` — this one can be active immediately since the stub already implements it: `expect(container.firstChild).toBeNull()` when mockCallStatus is `'idle'`
    - `it.skip('CALL-03: clicking Accept button calls acceptCall()')`
    - `it.skip('CALL-03: clicking Reject button calls rejectCall()')`
    - `it.skip('CALL-02: Escape key on backdrop calls rejectCall()')` (per UI-SPEC §9 accessibility)
    Make the "renders nothing when not ringing" test active (no skip) so this file has at least one passing test against the stub.

    Create `frontend/src/test/CallPage.test.tsx`. Mock `@/contexts/CallContext` returning mock streams + `hangUp: vi.fn()`. Render `<CallPage />` wrapped in `<MemoryRouter initialEntries={['/call']}>`. Scaffold tests:
    - `it.skip('UI-03: renders remote <video> with aria-label="Remote video stream"')`
    - `it.skip('UI-03: renders local <video> with aria-label="Local video preview" and muted attribute')`
    - `it.skip('UI-03: hang-up button has aria-label="End call"')`
    - `it.skip('UI-03: clicking hang-up button calls hangUp()')`
    - `it.skip('UI-03: shows "Waiting for remote video..." placeholder when remoteStream is null')`
    All five start as `it.skip` — Plan 04 will fill them in.

    Use the SignalMessage import for type assertions where needed — declare a local interface matching `{ to: string; type: string; from: string; payload: string }` in the test file or reference an existing type if one is exported from `@/contexts/CallContext`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/test/CallContext.test.tsx` exists and contains at least 8 `it.skip(` occurrences covering CALL-01 (x2 — outgoing + self-guard), CALL-02, CALL-03, CALL-04 (x2), CALL-07, CALL-08
    - `frontend/src/test/CallContext.test.tsx` contains the literal strings `'CALL-01'`, `'CALL-02'`, `'CALL-03'`, `'CALL-04'`, `'CALL-07'`, `'CALL-08'` in `it.skip` descriptions
    - `frontend/src/test/IncomingCallModal.test.tsx` contains `vi.mock('@/contexts/CallContext'` and `vi.mock('@/hooks/useRingtone'`
    - `frontend/src/test/IncomingCallModal.test.tsx` contains at least one active (non-skipped) `it(` test that passes against the stub
    - `frontend/src/test/CallPage.test.tsx` contains `MemoryRouter` import and at least 5 `it.skip(` occurrences referencing `'UI-03'`
    - Running `npm test -- --run` from `frontend/` exits 0 — total test count increases by at least 1 (the active "renders nothing when not ringing" test); all skipped tests are reported as skipped, not failing
    - `cd frontend && npx tsc --noEmit` exits 0 (no type errors in new test files)
  </acceptance_criteria>
  <done>Three test scaffolds in place; suite still green; Wave 0 gaps from VALIDATION.md closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → STOMP backend | Existing boundary from Phase 2; this plan does not introduce signaling logic |
| App shell ↔ React Router | New boundary: navigation triggered by CallContext via useNavigate (D-07). React Router controls navigation safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-W0-01 | Tampering | RTCPeerConnection global mock | accept | Mock is test-only (`vi.stubGlobal` in `setup.ts`); never ships to production. Production browser provides real RTCPeerConnection. |
| T-4-W0-02 | Information Disclosure | Toast container reads `useCall().toasts` | accept | Toast content originates from CallContext (local to user). No cross-user leak. Plan 02 owns toast content. |
| T-4-W0-03 | Denial of Service | StrictMode double-mount of provider tree | mitigate | D-04 provider order is deterministic; CallContext stub has no side effects. Plan 02 must add StrictMode guards per RESEARCH Pattern 5. |
| T-4-SC | Tampering | npm installs | mitigate | Phase 4 installs NO new packages (verified in RESEARCH §Package Legitimacy Audit); no slopcheck checkpoint needed. |
</threat_model>

<verification>
After all three tasks:
- `cd frontend && npm test -- --run` exits 0
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run build` exits 0
- Provider order in `main.tsx` matches D-04 verbatim
- All stub component files compile and are importable
- Test scaffolds report skipped tests in the verbose reporter output, not failures
</verification>

<success_criteria>
- Wave 0 gaps from `04-VALIDATION.md` (CallContext.test.tsx, IncomingCallModal.test.tsx, CallPage.test.tsx) all closed
- `nyquist_compliant` precondition met: every downstream Wave 1+ task has an automated test file to write against
- Plan 02 can implement `CallContext.tsx` body without touching `main.tsx`, `App.tsx`, or any other file
- Plans 03 and 04 can implement `IncomingCallModal`/`useRingtone` and `CallPage` independently (no shared file modifications)
- Plan 05 can wire `UserListPage.tsx` without touching app shell files
</success_criteria>

<output>
Create `.planning/phases/04-1-1-call-core/04-01-SUMMARY.md` when done — record files created, mock patterns established, and any deviations from PATTERNS.md.
</output>
