---
phase: 03-react-auth-user-list
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/index.html
  - frontend/components.json
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/vite-env.d.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/utils.ts
  - frontend/src/contexts/AuthContext.tsx
  - frontend/src/contexts/WebSocketContext.tsx
  - frontend/src/components/ProtectedRoute.tsx
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/alert.tsx
  - frontend/src/components/ui/avatar.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/test/setup.ts
  - frontend/src/test/mocks/server.ts
  - frontend/src/test/mocks/handlers.ts
  - frontend/src/test/AuthContext.test.tsx
  - frontend/src/test/AuthPage.test.tsx
  - frontend/src/test/UserListPage.test.tsx
  - frontend/src/pages/AuthPage.tsx
  - frontend/src/pages/UserListPage.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
tags:
  - react
  - vite
  - typescript
  - shadcn
  - scaffold
  - test-infrastructure
user_setup: []
must_haves:
  truths:
    - "frontend/ directory exists at repo root with a working Vite + React + TS project"
    - "`npm run build --prefix frontend` exits 0"
    - "`npm run test --prefix frontend -- --run` exits 0 with at least 3 stub test files discovered"
    - "AuthContext provider exposes { token, username, dispatch } with token=null on first mount when localStorage is empty"
    - "Axios instance attaches Authorization: Bearer {token} header on every outbound request when localStorage['vdt_token'] is set"
    - "All 7 shadcn components are present under src/components/ui/ as .tsx files"
  artifacts:
    - path: "frontend/package.json"
      provides: "Frontend dependency manifest with test/build/dev scripts"
      contains: '"test": "vitest"'
    - path: "frontend/vite.config.ts"
      provides: "Vite config with @ path alias, /api+/ws proxy, vitest jsdom config"
      contains: "ws: true"
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind v3 config with content globs and emerald accent extension"
      contains: "emerald"
    - path: "frontend/src/index.css"
      provides: "Tailwind v3 directives + UI-SPEC §4 CSS variable override"
      contains: "@tailwind base"
    - path: "frontend/src/lib/api.ts"
      provides: "Axios instance with JWT request interceptor reading localStorage['vdt_token']"
      contains: "interceptors.request.use"
    - path: "frontend/src/contexts/AuthContext.tsx"
      provides: "Auth state via useReducer, initialized from localStorage, useAuth() hook"
      contains: "useReducer"
    - path: "frontend/src/contexts/WebSocketContext.tsx"
      provides: "STOMP client provider stub with connect/disconnect/subscribe/publish + onlineUsers state"
      contains: "@stomp/stompjs"
    - path: "frontend/src/components/ProtectedRoute.tsx"
      provides: "Route guard reading useAuth().token and redirecting to /login"
      contains: "Navigate"
    - path: "frontend/src/App.tsx"
      provides: "BrowserRouter with /login, /users (protected), / (token-aware redirect)"
      contains: "BrowserRouter"
    - path: "frontend/src/main.tsx"
      provides: "App entry mounting AuthProvider > WebSocketProvider > App"
      contains: "AuthProvider"
    - path: "frontend/src/test/setup.ts"
      provides: "Vitest setup with testing-library cleanup + jest-dom"
      contains: "@testing-library/jest-dom"
    - path: "frontend/src/test/mocks/handlers.ts"
      provides: "MSW handlers for /api/auth/login, /api/auth/register, /api/auth/logout"
      contains: "/api/auth/login"
    - path: "frontend/src/test/AuthContext.test.tsx"
      provides: "Stub tests for AuthContext (initState, LOGIN, LOGOUT)"
      contains: "AuthContext"
    - path: "frontend/src/test/AuthPage.test.tsx"
      provides: "Stub tests for AuthPage (login success, 401 error, loading state)"
      contains: "AuthPage"
    - path: "frontend/src/test/UserListPage.test.tsx"
      provides: "Stub tests for UserListPage (filters self, renders rows, empty state)"
      contains: "UserListPage"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/contexts/AuthContext.tsx"
      via: "AuthProvider wrap"
      pattern: "AuthProvider"
    - from: "frontend/src/main.tsx"
      to: "frontend/src/contexts/WebSocketContext.tsx"
      via: "WebSocketProvider wrap (inside AuthProvider)"
      pattern: "WebSocketProvider"
    - from: "frontend/src/lib/api.ts"
      to: "localStorage['vdt_token']"
      via: "Axios request interceptor"
      pattern: "vdt_token"
    - from: "frontend/src/components/ProtectedRoute.tsx"
      to: "frontend/src/contexts/AuthContext.tsx"
      via: "useAuth() hook"
      pattern: "useAuth"
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8080"
      via: "dev server proxy for /api and /ws"
      pattern: "localhost:8080"
---

<objective>
Bootstrap the entire `frontend/` Vite + React 18 + TypeScript project with Tailwind v3, shadcn/ui (pinned to 2.3.0 for Tailwind v3 compatibility per D-01 and RESEARCH.md), all runtime dependencies (per D-02, D-04, D-08, D-10), the Axios instance with JWT interceptor (per D-05), AuthContext (per D-04), WebSocketContext (per D-08), ProtectedRoute (per D-03), test infrastructure (Vitest + Testing Library + MSW), and failing/stub tests for the two vertical slices that follow.

This is the foundational scaffold plan — no end-user feature is shipped yet, but every subsequent plan in this phase plugs implementations into wiring already created here. After this plan, `npm run build --prefix frontend` passes and `npm run test --prefix frontend -- --run` passes (stub tests are minimal smoke tests that confirm the scaffolding compiles and the test harness runs).

Purpose: Eliminate scaffolding overhead from Wave 1 and Wave 2 vertical-slice plans so they can each focus on a single user-visible capability without setup friction. Per the Anti-Shallow rules and the planner-mvp-mode reference, scaffolding that does not by itself deliver user-visible behavior is grouped into Wave 0 once.

Output: A buildable, testable, lint-clean React frontend with all providers, contexts, libraries, and test stubs wired but with empty/placeholder pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/03-react-auth-user-list/03-CONTEXT.md
@.planning/phases/03-react-auth-user-list/03-RESEARCH.md
@.planning/phases/03-react-auth-user-list/03-PATTERNS.md
@.planning/phases/03-react-auth-user-list/03-UI-SPEC.md
@.planning/phases/03-react-auth-user-list/03-VALIDATION.md
@backend/src/main/java/com/vdt/websocket/JwtChannelInterceptor.java
@backend/src/main/java/com/vdt/websocket/dto/PresenceDTO.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React-TS project + install all dependencies + Tailwind v3 + shadcn@2.3.0 init + add 7 shadcn components</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tailwind.config.js, frontend/postcss.config.js, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/index.html, frontend/components.json, frontend/src/index.css, frontend/src/components/ui/tabs.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/label.tsx, frontend/src/components/ui/button.tsx, frontend/src/components/ui/alert.tsx, frontend/src/components/ui/avatar.tsx, frontend/src/components/ui/skeleton.tsx, frontend/src/lib/utils.ts, frontend/src/vite-env.d.ts</files>
  <read_first>
    - .planning/phases/03-react-auth-user-list/03-RESEARCH.md (full file — installation sequence in §Standard Stack and Pattern 1)
    - .planning/phases/03-react-auth-user-list/03-UI-SPEC.md §1 (shadcn init prompts), §4 (CSS variable block — verbatim copy), §7 (component inventory)
    - .planning/phases/03-react-auth-user-list/03-PATTERNS.md (vite.config.ts, tailwind.config.js, index.css verbatim patterns)
    - CLAUDE.md §Technology Stack (Frontend table — confirms React 18, Vite 5, @stomp/stompjs 7.x, Axios 1.x, no Redux)
  </read_first>
  <action>
    Execute the exact 6-step install sequence from RESEARCH.md §Standard Stack at the repo root (D:/VDT-WebRTC).

    1. Run `npm create vite@latest frontend -- --template react-ts` (creates `frontend/`).
    2. `cd frontend && npm install` (baseline install).
    3. Install runtime deps per D-02/D-04/D-08/D-10: `npm install react-router-dom@6 @stomp/stompjs@7 axios@1 jwt-decode@4`. Do NOT install `sockjs-client` (forbidden per CLAUDE.md and RESEARCH.md Anti-Patterns).
    4. Install Tailwind v3 + postcss + @types/node: `npm install -D tailwindcss@^3.4 postcss autoprefixer @types/node`. Then `npx tailwindcss init -p` to generate `tailwind.config.js` and `postcss.config.js`.
    5. Install testing deps: `npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom msw`.
    6. Initialize shadcn pinned to 2.3.0 (NOT @latest — UI-SPEC says "Tailwind CSS 3.x via shadcn init" and RESEARCH.md Pitfall 1 documents the v4 incompatibility): `npx shadcn@2.3.0 init`. Answer prompts: Style=Default, Base color=Slate, CSS variables=Yes.
    7. Add the 7 shadcn components per UI-SPEC §7: `npx shadcn@2.3.0 add tabs input label button alert avatar skeleton`.

    Then write/overwrite the following config files with the exact content given in 03-PATTERNS.md:
    - `frontend/tailwind.config.js` — content globs `./index.html` + `./src/**/*.{js,ts,jsx,tsx}`; theme.extend.colors.accent = { DEFAULT: '#10B981', hover: '#059669', muted: '#064E3B' } per UI-SPEC §1.
    - `frontend/src/index.css` — Tailwind v3 directives (`@tailwind base; @tailwind components; @tailwind utilities;`) followed verbatim by the UI-SPEC §4 CSS variable override block (`:root, .dark { --background: 222 84% 5%; ... --radius: 0.5rem; }`) and the `html, body, #root` background rule. Do NOT write `@import "tailwindcss"` (that is v4).
    - `frontend/vite.config.ts` — defineConfig with plugins=[react()], resolve.alias={ "@": path.resolve(__dirname, "./src") }, server.proxy={'/api': { target: 'http://localhost:8080', changeOrigin: true }, '/ws': { target: 'ws://localhost:8080', ws: true, changeOrigin: true }}, test={ globals: true, environment: 'jsdom', setupFiles: './src/test/setup.ts' }.
    - `frontend/tsconfig.json` and `frontend/tsconfig.app.json` — add compilerOptions.baseUrl="." and compilerOptions.paths={ "@/*": ["./src/*"] }. Add types=["vitest/globals", "@testing-library/jest-dom"] in tsconfig.app.json compilerOptions.
    - `frontend/package.json` — add `"test": "vitest"` to scripts; verify existing `"build": "tsc -b && vite build"` and `"dev": "vite"` scripts exist.
    - `frontend/index.html` — add `<link>` to Inter font from Google Fonts per UI-SPEC §1 (preconnect + Inter weights 400, 600).

    Verify the scaffold builds: `npm run build --prefix frontend` must exit 0. If shadcn init prompts for `components.json` path or import alias, accept defaults (uses the `@/` alias just configured).
  </action>
  <acceptance_criteria>
    - `frontend/package.json` exists and `cat frontend/package.json | grep -E '"(react|vite|@stomp/stompjs|axios|jwt-decode|tailwindcss|shadcn|vitest|@testing-library/react|msw)"'` returns at least 10 lines
    - `frontend/package.json` contains `"test": "vitest"` (grep `"test":` returns the vitest line)
    - `frontend/tailwind.config.js` exists; grep returns matches for both `content:` and `emerald` (or `#10B981`)
    - `frontend/src/index.css` exists; grep `-v '^[[:space:]]*\\*\\|^[[:space:]]*//\\|^[[:space:]]*/\\*' frontend/src/index.css | grep -c '@tailwind base'` returns 1 and grep returns no match for `@import "tailwindcss"`
    - `frontend/vite.config.ts` contains all of: `ws: true`, `target: 'ws://localhost:8080'`, `environment: 'jsdom'`, `'@': path.resolve`
    - `frontend/tsconfig.json` and `frontend/tsconfig.app.json` both contain `"@/*"` in paths
    - `frontend/src/components/ui/tabs.tsx`, `input.tsx`, `label.tsx`, `button.tsx`, `alert.tsx`, `avatar.tsx`, `skeleton.tsx` all exist
    - `frontend/index.html` contains `fonts.googleapis.com` and `Inter`
    - `npm run build --prefix frontend` exits 0
    - No file under `frontend/node_modules/sockjs-client/` exists (`ls frontend/node_modules/sockjs-client 2>$null` returns nothing)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/VDT-WebRTC/frontend && npm run build</automated>
  </verify>
  <done>
    Vite project scaffolds, Tailwind v3 + shadcn@2.3.0 + 7 components present, all runtime + test dependencies installed, dev/build scripts work, `npm run build` produces dist/ with 0 errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement core wiring — Axios instance, AuthContext, WebSocketContext, ProtectedRoute, App router, main entry — and write Wave 0 test stubs</name>
  <files>frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/lib/api.ts, frontend/src/contexts/AuthContext.tsx, frontend/src/contexts/WebSocketContext.tsx, frontend/src/components/ProtectedRoute.tsx, frontend/src/pages/AuthPage.tsx, frontend/src/pages/UserListPage.tsx, frontend/src/test/setup.ts, frontend/src/test/mocks/handlers.ts, frontend/src/test/mocks/server.ts, frontend/src/test/AuthContext.test.tsx, frontend/src/test/AuthPage.test.tsx, frontend/src/test/UserListPage.test.tsx</files>
  <read_first>
    - frontend/src/main.tsx (Vite default — to be overwritten)
    - frontend/src/App.tsx (Vite default — to be overwritten)
    - .planning/phases/03-react-auth-user-list/03-PATTERNS.md (verbatim implementations for AuthContext, WebSocketContext, ProtectedRoute, App.tsx, main.tsx, api.ts)
    - .planning/phases/03-react-auth-user-list/03-RESEARCH.md Patterns 2-5 and Pitfalls 2, 3, 4, 7
    - .planning/phases/03-react-auth-user-list/03-CONTEXT.md (decisions D-04 through D-12 — locked behavior)
    - .planning/phases/03-react-auth-user-list/03-VALIDATION.md (test stub requirements)
    - backend/src/main/java/com/vdt/websocket/JwtChannelInterceptor.java (confirms STOMP header reading pattern)
    - backend/src/main/java/com/vdt/websocket/dto/PresenceDTO.java (confirms `{ onlineUsers: string[] }` shape)
  </read_first>
  <behavior>
    - AuthContext initState reads localStorage['vdt_token'] synchronously; if present, decodes JWT `sub` claim via jwtDecode (named import from 'jwt-decode'); on decode failure, removes the bad token from localStorage and returns { token: null, username: null }.
    - AuthContext LOGIN action: sets localStorage['vdt_token']=token, returns { token, username }.
    - AuthContext LOGOUT action: removes localStorage['vdt_token'], returns { token: null, username: null }.
    - useAuth() throws "useAuth must be used inside AuthProvider" if not wrapped.
    - Axios interceptor reads token from localStorage['vdt_token'] (NOT from React state — per D-05 discretion and Pattern 5) on every request, sets config.headers.Authorization=`Bearer ${token}` when present.
    - WebSocketContext.connect(token) creates a Client with brokerURL='ws://localhost:8080/ws', connectHeaders={ Authorization: `Bearer ${token}` }, reconnectDelay=5000, onConnect callback subscribes to /topic/presence inside onConnect (per Pitfall 2), JSON.parses frame.body, sets onlineUsers state.
    - WebSocketContext.disconnect() calls client.deactivate(), clears onlineUsers to [], sets client to null.
    - ProtectedRoute returns `<Navigate to="/login" replace />` when token is falsy; renders children otherwise.
    - App.tsx routes: /login → AuthPage, /users → ProtectedRoute > UserListPage, / → Navigate to token ? /users : /login (per D-02).
    - main.tsx mounts <AuthProvider><WebSocketProvider><App/></WebSocketProvider></AuthProvider> inside React.StrictMode (provider order matters — WebSocketContext can read from AuthContext later).
    - Stub AuthPage component renders a minimal placeholder `<div>` (one h1 "Auth"); Stub UserListPage renders `<div>` with one h1 "Users" — full implementations land in plans 02 and 03.
    - Test stubs: AuthContext.test.tsx covers initState empty + initState with valid token + LOGIN dispatch + LOGOUT dispatch (4 tests min). AuthPage.test.tsx contains at least 1 placeholder test that passes (`expect(true).toBe(true)` with TODO comment referencing UI-01 — replaced in Plan 02). UserListPage.test.tsx contains at least 1 placeholder test (TODO referencing UI-02 — replaced in Plan 03). All stub tests pass with `npm run test -- --run`.
  </behavior>
  <action>
    Implement the following files using the verbatim patterns from 03-PATTERNS.md (do NOT paraphrase types/identifiers — they must match downstream test mocks):

    1. `frontend/src/lib/api.ts` — Axios instance per Pattern 5. baseURL='http://localhost:8080'. Token key constant `'vdt_token'`. Export default `api`.
    2. `frontend/src/contexts/AuthContext.tsx` — Pattern 2 verbatim. Exports: `AuthProvider`, `useAuth` hook, `AuthState` and `AuthAction` types. Reducer handles 'LOGIN' and 'LOGOUT'. `initState` uses jwtDecode (named import).
    3. `frontend/src/contexts/WebSocketContext.tsx` — Pattern 3 verbatim. Exports: `WebSocketProvider`, `useWebSocket` hook. Provides `{ client, onlineUsers, isLoading, connect, disconnect, subscribe, publish }`. Subscription to /topic/presence MUST be inside onConnect callback (Pitfall 2). onStompError logs and sets isLoading=false.
    4. `frontend/src/components/ProtectedRoute.tsx` — Pattern 4 verbatim. Uses `<Navigate to="/login" replace />`.
    5. `frontend/src/App.tsx` — Three routes per D-02 (`/login`, `/users` protected, `/` token-aware redirect). Use `useAuth()` to read token for root redirect.
    6. `frontend/src/main.tsx` — Provider wrap order: `<AuthProvider><WebSocketProvider><App/></WebSocketProvider></AuthProvider>` inside StrictMode. Imports `./index.css`.
    7. `frontend/src/pages/AuthPage.tsx` — Stub: `export default function AuthPage() { return <div><h1>Auth</h1></div> }`. Replaced in Plan 02.
    8. `frontend/src/pages/UserListPage.tsx` — Stub: `export default function UserListPage() { return <div><h1>Users</h1></div> }`. Replaced in Plan 03.
    9. `frontend/src/test/setup.ts` — Pattern 7 verbatim: import '@testing-library/jest-dom', cleanup() in afterEach.
    10. `frontend/src/test/mocks/handlers.ts` — MSW handlers for POST /api/auth/login (returns `{ token: 'mock.jwt.token' }`), POST /api/auth/register (returns `{ token: 'mock.jwt.token' }`), POST /api/auth/logout (returns 200). Export `handlers` array.
    11. `frontend/src/test/mocks/server.ts` — `setupServer(...handlers)` from msw/node; export `server`. Note: handlers can use a real-looking JWT payload encoded as base64 for `sub` claim decode to succeed in tests (e.g., header.eyJzdWIiOiJ0ZXN0dXNlciJ9.sig where the middle part decodes to `{"sub":"testuser"}`).
    12. `frontend/src/test/AuthContext.test.tsx` — 4 tests: (a) initial state has token=null when localStorage empty, (b) initial state restores token + username from localStorage (set `localStorage.setItem('vdt_token', validJwt)` in beforeEach), (c) LOGIN dispatch sets token and localStorage, (d) LOGOUT dispatch clears token and localStorage. Use `renderHook` from @testing-library/react.
    13. `frontend/src/test/AuthPage.test.tsx` — Placeholder file with one passing test plus `it.todo('UI-01: login success navigates to /users')`, `it.todo('UI-01: 401 shows error alert')`, `it.todo('UI-01: 409 shows username taken')`, `it.todo('UI-01: submit button shows spinner while loading')`. The todos become real tests in Plan 02.
    14. `frontend/src/test/UserListPage.test.tsx` — Placeholder file with one passing test plus `it.todo('UI-02: renders online users')`, `it.todo('UI-02: filters self from list')`, `it.todo('UI-02: shows skeleton while loading')`, `it.todo('UI-02: shows empty state when no other users')`. Real tests land in Plan 03.

    Delete the Vite default `App.css` if present (project uses Tailwind only). Verify imports use `@/` alias not relative paths.

    Run `npm run test --prefix frontend -- --run`. The 4 AuthContext tests must pass; the 2 placeholder test files must pass with their single `expect(true).toBe(true)` test plus all `it.todo` entries marked as pending. Total pass count >= 4 with 0 failures.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` contains `interceptors.request.use` and the literal string `'vdt_token'`
    - `frontend/src/contexts/AuthContext.tsx` contains `useReducer`, `jwtDecode`, `'vdt_token'`, and exports `useAuth`
    - `frontend/src/contexts/WebSocketContext.tsx` contains `new Client(`, `connectHeaders:`, `'ws://localhost:8080/ws'`, and `'/topic/presence'`; the `subscribe('/topic/presence'` call is inside an `onConnect:` block (grep multiline) — NOT at the top level after `activate()`
    - `frontend/src/contexts/WebSocketContext.tsx` does NOT contain `sockjs` (case-insensitive)
    - `frontend/src/components/ProtectedRoute.tsx` contains `<Navigate to="/login" replace />` (or equivalent with `replace`) and `useAuth`
    - `frontend/src/App.tsx` contains `BrowserRouter`, `/login`, `/users`, `ProtectedRoute`
    - `frontend/src/main.tsx` contains `AuthProvider` wrapping `WebSocketProvider` wrapping `<App`
    - `frontend/src/test/setup.ts` imports `@testing-library/jest-dom` and calls `cleanup`
    - `frontend/src/test/mocks/handlers.ts` defines handlers for `/api/auth/login`, `/api/auth/register`, `/api/auth/logout`
    - `frontend/src/test/AuthContext.test.tsx` contains at least 4 `it(` or `test(` blocks
    - `npm run test --prefix frontend -- --run` exits 0 with at least 4 passing tests and 0 failures (output line `Tests` shows `passed` and no `failed`)
    - `npm run build --prefix frontend` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/VDT-WebRTC/frontend && npm run build && npm run test -- --run</automated>
  </verify>
  <done>
    All scaffold files exist; AuthContext + Axios interceptor + WebSocketContext + ProtectedRoute + App router + main entry are wired; test infrastructure runs; AuthContext tests pass (4 real tests); AuthPage/UserListPage have stub tests with `it.todo` placeholders for Plans 02/03; TypeScript build is clean.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → REST API | Untrusted form input crosses HTTPS into Spring Boot /api/auth/* endpoints |
| Browser → STOMP /ws | Untrusted JWT from localStorage is presented in STOMP CONNECT headers |
| Vite dev server → Backend | Dev proxy forwards /api and /ws to localhost:8080 (LAN only) |
| localStorage | Cross-origin attacker via XSS could read localStorage['vdt_token'] |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Information Disclosure | localStorage JWT token | MEDIUM | accept | LAN demo scope per CONTEXT.md D-05; mitigated by: no `dangerouslySetInnerHTML`, no `eval`, no innerHTML assignment anywhere in scaffold; AuthContext rejects malformed tokens and removes them. Production path: httpOnly cookies (v2 — AUTH-V2-01). |
| T-3-02 | Spoofing | STOMP CONNECT with stolen token | MEDIUM | mitigate | Token is placed in STOMP `connectHeaders.Authorization` (not URL params) per RESEARCH Pitfall 3 — verified against backend `JwtChannelInterceptor.java` lines 41-44. Brokers must reject malformed/expired JWTs (already enforced by Phase 2 interceptor). |
| T-3-03 | Tampering | Axios interceptor reads token from localStorage | LOW | mitigate | Interceptor reads only the `vdt_token` key; no other origins write to it; HTTPS in production prevents MITM (LAN demo accepts plain ws://). |
| T-3-04 | Information Disclosure | JWT in URL/query params | LOW | mitigate | Axios uses Authorization header only; STOMP uses connectHeaders only; ProtectedRoute uses hardcoded redirect (no `?redirect=` open redirect). |
| T-3-05 | Tampering | npm package installs | LOW | mitigate | All packages verified via npm registry per RESEARCH §Package Legitimacy Audit; correct scope `@stomp/stompjs` (not `@stompjs/stompjs` typo per RESEARCH Anti-Patterns); `sockjs-client` explicitly excluded. |
| T-3-06 | Elevation of Privilege | Unauthenticated route access | LOW | mitigate | ProtectedRoute wrapper checks `useAuth().token` before rendering; redirects to /login with `replace: true` (back-button safe). Backend enforces JWT independently — frontend guard is UX, not security boundary. |

**HIGH severity items:** none in this scaffold plan. T-3-01 is MEDIUM and explicitly accepted per CONTEXT.md D-05 (LAN demo scope, 24h tokens, no PII beyond username).

</threat_model>

<verification>
- `npm run build --prefix frontend` exits 0
- `npm run test --prefix frontend -- --run` exits 0 with >=4 passing tests
- `frontend/src/components/ui/` contains exactly 7 .tsx files: tabs, input, label, button, alert, avatar, skeleton
- `grep -r 'sockjs' frontend/src` returns no matches (case-insensitive)
- `grep -r '@import "tailwindcss"' frontend/src` returns no matches (v4 syntax must be absent)
- AuthContext test for malformed token in localStorage: the bad token is removed and state is { token: null, username: null }
</verification>

<success_criteria>
- Foundational frontend scaffold is complete and buildable
- All 4 contexts/utilities (AuthContext, WebSocketContext, api.ts, ProtectedRoute) are in place with full implementations matching CONTEXT.md decisions D-04 through D-12
- Test infrastructure (Vitest + RTL + MSW + jsdom) runs and AuthContext has 4 passing unit tests
- Stub tests for AuthPage and UserListPage exist with `it.todo` entries pre-declaring the assertions Plans 02 and 03 will fulfill
- Provider tree is wired in main.tsx (AuthProvider > WebSocketProvider > App)
- App router has the 3 routes per D-02 with ProtectedRoute guarding /users
- No banned dependencies (sockjs-client) and no banned syntax (Tailwind v4 `@import`)
</success_criteria>

<output>
Create `.planning/phases/03-react-auth-user-list/03-01-SUMMARY.md` documenting: scaffold completed, dependencies installed (with versions captured from package.json), shadcn components added, AuthContext + WebSocketContext + ProtectedRoute + Axios interceptor implementations in place, test infrastructure operational with N tests passing and M todos pending for Plans 02/03.
</output>
