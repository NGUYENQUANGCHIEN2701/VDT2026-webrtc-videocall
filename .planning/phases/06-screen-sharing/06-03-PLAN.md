---
phase: 06-screen-sharing
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - frontend/src/pages/CallPage.tsx
autonomous: true
requirements: [SCRN-01, SCRN-03]
must_haves:
  truths:
    - "A Share button appears in the control bar; clicking it (idle) starts screen sharing, clicking it (active) stops it (SCRN-01, SCRN-03)"
    - "The Share button shows Monitor/slate when idle and MonitorOff/emerald when sharing (D-03)"
    - "The Camera button is disabled while screen sharing and re-enables when sharing stops (D-05)"
    - "The local PiP keeps showing the camera feed during screen share (D-04)"
  artifacts:
    - path: "frontend/src/pages/CallPage.tsx"
      provides: "Share button in control bar, updated Camera disabled condition, new COPY entries, Monitor/MonitorOff imports"
      contains: "Share screen"
  key_links:
    - from: "frontend/src/pages/CallPage.tsx Share button onClick"
      to: "CallContext startScreenShare / stopScreenShare"
      via: "useCall() destructure"
      pattern: "isScreenSharing \\? stopScreenShare : startScreenShare"
    - from: "frontend/src/pages/CallPage.tsx Camera button"
      to: "isScreenSharing"
      via: "disabled prop"
      pattern: "disabled=\\{!hasVideoTracks \\|\\| isScreenSharing\\}"
---

<objective>
Add the Share Screen button to the CallPage control bar and update the Camera button to disable while sharing. The control bar grows from 3 buttons (`Mic · End · Camera`) to 4 (`Mic · Share · End · Camera`, D-01), with End Call as the center anchor. Wire the Share button to `startScreenShare`/`stopScreenShare` from CallContext (delivered in Plan 02), apply the D-03 idle/active visual states, and gate the Camera button on `isScreenSharing` (D-05).

Purpose: Completes the user-facing surface of Phase 6. After this plan a user can click Share during a live call and the remote peer sees their screen (SCRN-01); clicking again restores the camera (SCRN-03). SCRN-04 (remote sees it automatically) is delivered by Plan 02's replaceTrack and verified via the manual two-tab UAT.
Output: Modified `CallPage.tsx` with the Share button, updated Camera button disabled logic, two new COPY entries, and `Monitor`/`MonitorOff` added to the lucide-react import. The Plan 01 RED CallPage cases turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-screen-sharing/06-UI-SPEC.md
@.planning/phases/06-screen-sharing/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COPY entries, Monitor/MonitorOff imports, and consume isScreenSharing/startScreenShare/stopScreenShare from useCall</name>
  <files>frontend/src/pages/CallPage.tsx</files>
  <read_first>
    - frontend/src/pages/CallPage.tsx (the lucide import at line 10; the `COPY` const at lines 17-25; the `useCall()` destructure at lines 45-56)
    - frontend/src/contexts/CallContext.tsx (the `CallContextValue` interface — confirm `isScreenSharing`, `startScreenShare`, `stopScreenShare` are exported by Plan 02 before consuming)
    - .planning/phases/06-screen-sharing/06-UI-SPEC.md (§8 copy table lines 313-326 for exact label strings; §6 icon table lines 271-289)
  </read_first>
  <action>
    In `CallPage.tsx`:
    1. Update the lucide-react import (line 10) from `import { Users, PhoneOff, Mic, MicOff, Video, VideoOff } from 'lucide-react'` to also include `Monitor` and `MonitorOff` — final: `import { Users, PhoneOff, Mic, MicOff, Video, VideoOff, Monitor, MonitorOff } from 'lucide-react'`. (Both icons are confirmed present in lucide-react 1.16 per UI-SPEC §6.)
    2. Add two entries to the `COPY` const (lines 17-25), copying the exact strings from UI-SPEC §8 — do not paraphrase:
       - `shareScreenLabel: 'Share screen'`
       - `stopSharingLabel: 'Stop sharing'`
       - `cameraDisabledSharingLabel: 'Camera disabled while sharing'`
    3. Extend the `useCall()` destructure (lines 45-56) to also pull `isScreenSharing`, `startScreenShare`, `stopScreenShare`.
    Do not yet render the Share button or change the Camera button (Task 2) — but the destructured values must be used by end of plan to avoid unused-variable lint errors, so it is acceptable to complete Task 2 in the same execution pass.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lucide import line contains `Monitor` and `MonitorOff` (source assertion)
    - `COPY` contains `shareScreenLabel: 'Share screen'`, `stopSharingLabel: 'Stop sharing'`, `cameraDisabledSharingLabel: 'Camera disabled while sharing'` (source assertion — exact strings)
    - `useCall()` destructure includes `isScreenSharing`, `startScreenShare`, `stopScreenShare` (source assertion)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Icons imported, exact COPY strings added, screen-share context values destructured.</done>
</task>

<task type="auto">
  <name>Task 2: Render Share button (2nd from left) and gate Camera button on isScreenSharing</name>
  <files>frontend/src/pages/CallPage.tsx</files>
  <read_first>
    - frontend/src/pages/CallPage.tsx (the control-bar JSX at lines 148-180 — the Mic button 151-158, End Call 161-167, Camera button 170-179; the bar container at line 149 with `gap-4`)
    - .planning/phases/06-screen-sharing/06-UI-SPEC.md (§5.1 button order line 147; §5.2 Share button state table lines 162-174 including aria-pressed and "never disabled"; §5.2 Camera updated disabled condition lines 189-207; §9 accessibility lines 356-367)
    - .planning/phases/06-screen-sharing/06-RESEARCH.md (Code Examples: Share button JSX lines 432-449; updated Camera button lines 451-481)
  </read_first>
  <action>
    Edit the control bar (lines 148-180) so the left-to-right order is Mic → Share → End Call → Camera (D-01). Insert the Share button between the existing Mic button (ends line 158) and the End Call button (starts line 161).

    Share button (shadcn `Button`, icon-only, follows the Mic button class shape `h-10 w-10 rounded-full text-white transition-colors duration-150`):
    - className conditional on `isScreenSharing`: when true → `bg-emerald-600 hover:bg-emerald-700`; when false → `bg-slate-700 hover:bg-slate-600` (D-03 — emerald-600 is the new active background)
    - `aria-label={isScreenSharing ? COPY.stopSharingLabel : COPY.shareScreenLabel}`
    - `title={isScreenSharing ? COPY.stopSharingLabel : COPY.shareScreenLabel}` (tooltip per UI-SPEC §8)
    - `aria-pressed={isScreenSharing}`
    - `onClick={isScreenSharing ? stopScreenShare : startScreenShare}`
    - icon: `{isScreenSharing ? <MonitorOff className="size-4" /> : <Monitor className="size-4" />}`
    - The Share button is NEVER disabled (UI-SPEC §5.2) — do not add a `disabled` prop.

    Update the Camera button (lines 170-179) to add `isScreenSharing` as a second disabled condition (D-05):
    - `disabled={!hasVideoTracks || isScreenSharing}` and `aria-disabled={!hasVideoTracks || isScreenSharing}`
    - className disabled branch fires on `!hasVideoTracks || isScreenSharing` → `bg-slate-700 opacity-50 cursor-not-allowed` (keep the existing isCameraOff/active branches for the enabled case)
    - aria-label precedence: `!hasVideoTracks ? COPY.cameraUnavailableLabel : isScreenSharing ? COPY.cameraDisabledSharingLabel : isCameraOff ? COPY.cameraOnLabel : COPY.cameraOffLabel`
    - The icon while disabled-by-sharing reflects the current camera state (Video if camera on, VideoOff if off) — do NOT force VideoOff when the reason is `isScreenSharing` (UI-SPEC §5.2 row 4). Keep the existing `{isCameraOff || !hasVideoTracks ? <VideoOff .../> : <Video .../>}` icon expression, which already yields Video when camera is on and not unavailable.

    Do NOT change the local PiP `<video>` (lines 137-145) or its `srcObject` useEffect (lines 68-70) — the PiP must continue binding `localStream` (D-04). Do NOT change the bar container `gap-4` (4 buttons fit the existing spacing per UI-SPEC §5.2).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run CallPage</automated>
  </verify>
  <acceptance_criteria>
    - Share button renders with aria-label `Share screen` when idle and `Stop sharing` when sharing (behavior: Plan 01 SCRN-01 render cases pass)
    - Clicking the Share button calls `startScreenShare` when idle and `stopScreenShare` when active (behavior: Plan 01 click cases pass)
    - Share button has `aria-pressed` reflecting `isScreenSharing` (behavior: Plan 01 D-03 case passes)
    - Camera button is disabled when `isScreenSharing` is true with aria-label `Camera disabled while sharing` (behavior: Plan 01 D-05 case passes)
    - Control bar source contains `isScreenSharing ? stopScreenShare : startScreenShare` and `bg-emerald-600 hover:bg-emerald-700` (source assertion)
    - Camera button source contains `disabled={!hasVideoTracks || isScreenSharing}` (source assertion)
    - Local PiP `srcObject` effect still binds `localStream` only — no `setLocalStream`/screen stream change in CallPage (source assertion: D-04 preserved)
    - `npm run test -- --run CallPage` exits 0 (Plan 01 CallPage screen-share cases + all prior Phase 4/5 CallPage cases green)
  </acceptance_criteria>
  <done>Share button rendered 2nd-from-left with D-03 states wired to start/stopScreenShare; Camera button disabled-while-sharing per D-05; PiP unchanged (D-04); CallPage test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → CallPage controls | User click events trigger CallContext screen-share actions; no untrusted external input enters here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Spoofing / UX confusion | Share button state misread by user | mitigate | `aria-pressed={isScreenSharing}` + dynamic `aria-label` (Share screen / Stop sharing) + `title` tooltip communicate toggle state to sighted and screen-reader users (UI-SPEC §9). Distinct emerald-600 active color (4.6:1 AA contrast). |
| T-06-08 | Information Disclosure | accidental share start with no obvious stop | accept | Stopping is fully reversible and always one click away (Share button never disabled). Browser also shows its native "Stop sharing" bar (handled by Plan 02 onended). Low risk. |
| T-06-09 | Tampering | npm installs | accept | No package installs — `Monitor`/`MonitorOff` already in installed lucide-react 1.16; zero new dependencies (06-RESEARCH.md). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run test -- --run CallPage` exits 0 — all Plan 01 CallPage screen-share RED cases now GREEN, no Phase 4/5 CallPage case regressed
- `cd frontend && npm run test -- --run` exits 0 — full frontend suite green (the phase gate)
- `cd frontend && npx tsc --noEmit` exits 0
- Manual two-tab UAT (SCRN-04, and D-06 native-stop): open app in two localhost tabs, A calls B, A clicks Share Screen and picks a window → B's remote video switches to the screen with no call drop (SCRN-04); A clicks Stop Sharing → B returns to camera (SCRN-03); A shares again then clicks the browser's native "Stop sharing" bar → A's Share button returns to idle and B returns to camera (D-06)
</verification>

<success_criteria>
- 4-button control bar (Mic · Share · End · Camera) with End Call center anchor (D-01)
- Share button toggles start/stopScreenShare with D-03 idle(slate/Monitor)/active(emerald/MonitorOff) states
- Camera button disabled while sharing, re-enables on stop (D-05)
- Local PiP keeps camera feed (D-04 — unchanged)
- Full frontend test suite green; zero new packages
</success_criteria>

<output>
Create `.planning/phases/06-screen-sharing/06-03-SUMMARY.md` when done
</output>
