---
phase: 06-screen-sharing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/test/setup.ts
  - frontend/src/test/CallContext.test.tsx
  - frontend/src/test/CallPage.test.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-04]
must_haves:
  truths:
    - "Test suite can mock getDisplayMedia, getSenders, and replaceTrack so screen-share code is testable"
    - "Failing test scaffolds exist for all SCRN-01 through D-08 behaviors before implementation"
  artifacts:
    - path: "frontend/src/test/setup.ts"
      provides: "getDisplayMedia mock, getSenders/replaceTrack on MockRTCPeerConnection, mockScreenTrack export"
      contains: "getDisplayMedia"
    - path: "frontend/src/test/CallContext.test.tsx"
      provides: "11 failing test cases for screen-share CallContext behavior"
      contains: "startScreenShare"
    - path: "frontend/src/test/CallPage.test.tsx"
      provides: "Share button render + Camera disabled-while-sharing test cases"
      contains: "Share screen"
  key_links:
    - from: "frontend/src/test/CallContext.test.tsx"
      to: "frontend/src/test/setup.ts"
      via: "import mockScreenTrack"
      pattern: "import.*mockScreenTrack.*from.*setup"
---

<objective>
Fill the Wave 0 test-infrastructure gaps so screen-sharing code (delivered in Plans 02 and 03) is testable, and author the failing test scaffolds that define expected behavior before implementation exists.

Purpose: The validation strategy (06-VALIDATION.md) marks all SCRN tests as "❌ W0" — the mocks they depend on do not yet exist in `setup.ts`. Without `getDisplayMedia`, `getSenders()`, and `replaceTrack` mocks, none of the screen-share unit tests can run. This plan creates that foundation plus the RED test cases.
Output: Extended `setup.ts` with three new mocks; new failing test cases in `CallContext.test.tsx` (11 cases) and `CallPage.test.tsx` (Share button + Camera-disabled-while-sharing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-screen-sharing/06-RESEARCH.md
@.planning/phases/06-screen-sharing/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend setup.ts with getDisplayMedia, getSenders, replaceTrack mocks and mockScreenTrack export</name>
  <files>frontend/src/test/setup.ts</files>
  <read_first>
    - frontend/src/test/setup.ts (current mock infrastructure — see existing mockAudioTrack/mockVideoTrack exports at lines 73-74, mockMediaStream at 75-79, and the vi.stubGlobal('navigator', ...) block at 81-86)
    - frontend/src/contexts/CallContext.tsx (lines 389-403 getLocalStream, lines 416-448 startCall, to confirm how addTrack/getSenders are consumed)
    - .planning/phases/06-screen-sharing/06-RESEARCH.md (Code Examples — "getDisplayMedia Mock for Tests" lines 501-528)
  </read_first>
  <action>
    Add to `frontend/src/test/setup.ts`:
    1. Export `mockScreenTrack` — an object literal `{ stop: vi.fn(), onended: null, kind: 'video' }`. Type `onended` as `(() => void) | null`. Place it alongside the existing `mockAudioTrack` / `mockVideoTrack` exports (after line 74).
    2. Add a module-scope `mockScreenStream` object with `getTracks` and `getVideoTracks` both returning `[mockScreenTrack]` via `vi.fn().mockReturnValue(...)`.
    3. In the existing `vi.stubGlobal('navigator', ...)` block (line 81), add a `getDisplayMedia: vi.fn().mockResolvedValue(mockScreenStream)` property to `mediaDevices`, parallel to the existing `getUserMedia` mock. Do NOT remove the existing `getUserMedia` mock.
    4. On the `MockRTCPeerConnection` class, add a `getSenders` field: `getSenders = vi.fn().mockReturnValue([{ track: mockVideoTrack, replaceTrack: vi.fn().mockResolvedValue(undefined) }])`. Place it alongside the existing `addTrack`/`createOffer` fields (after line 20). The sender's `track` must reference the exported `mockVideoTrack` so its `kind`/`enabled` shape matches the production `s.track?.kind === 'video'` lookup — add a `kind: 'video'` property to `mockVideoTrack` (it currently has only `stop`/`enabled`) so the `getSenders().find(s => s.track?.kind === 'video')` lookup in production code resolves to this sender.
    The replaceTrack mock must be reachable from tests — the test retrieves the sender via `(globalThis.RTCPeerConnection).lastInstance.getSenders()[0].replaceTrack`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `setup.ts` exports a `mockScreenTrack` const with `stop`, `onended`, and `kind: 'video'` properties (source assertion: file contains `export const mockScreenTrack`)
    - `setup.ts` `mediaDevices` block contains `getDisplayMedia: vi.fn().mockResolvedValue` (source assertion)
    - `MockRTCPeerConnection` contains a `getSenders = vi.fn().mockReturnValue` field whose array element has a `replaceTrack` function (source assertion: file contains `getSenders = vi.fn` and `replaceTrack`)
    - `mockVideoTrack` object includes `kind: 'video'` (source assertion)
    - Existing `getUserMedia` mock is still present (source assertion: file still contains `getUserMedia: vi.fn().mockResolvedValue(mockMediaStream)`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>setup.ts compiles with getDisplayMedia, getSenders, replaceTrack mocks and exports mockScreenTrack; existing getUserMedia mock untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Author failing CallContext screen-share test cases (SCRN-01/02/03/04, D-06, D-08)</name>
  <files>frontend/src/test/CallContext.test.tsx</files>
  <read_first>
    - frontend/src/test/CallContext.test.tsx (existing test patterns — note the `wrapper`, `simulateSignal`, `renderHook(() => useCall(), { wrapper })` pattern, the `MockPC.lastInstance` retrieval pattern at lines 412-419 and 680-693, and the CTRL-03 teardown test at lines 658-710)
    - frontend/src/test/setup.ts (imports — Task 1 adds `mockScreenTrack`; existing import line is `import { mockAudioTrack, mockVideoTrack } from './setup'` at line 6)
    - frontend/src/contexts/CallContext.tsx (the planned API surface: `isScreenSharing`, `startScreenShare`, `stopScreenShare` will be added in Plan 02 — write tests against that contract)
    - .planning/phases/06-screen-sharing/06-RESEARCH.md (Phase Requirements → Test Map lines 597-610; startScreenShare/stopScreenShare reference implementations lines 360-419)
  </read_first>
  <action>
    Add a new `describe('CallContext — screen sharing', ...)` block (or append `it` cases inside the existing `describe('CallContext', ...)`) in `CallContext.test.tsx`. Import `mockScreenTrack` from `./setup` (extend the existing import on line 6 to `import { mockAudioTrack, mockVideoTrack, mockScreenTrack } from './setup'`). Write these failing cases — each starts a call first via `await result.current.startCall('bob')` so a peer connection exists:
    1. `SCRN-01/02: startScreenShare calls getDisplayMedia and sets isScreenSharing to true` — call `startScreenShare()`, assert `navigator.mediaDevices.getDisplayMedia` was called with `{ video: true }` and `result.current.isScreenSharing === true`.
    2. `SCRN-02: startScreenShare calls replaceTrack on the video sender with the screen track` — retrieve `(globalThis.RTCPeerConnection).lastInstance.getSenders()[0].replaceTrack` and assert it was called with `mockScreenTrack`.
    3. `SCRN-03: stopScreenShare calls replaceTrack with the camera track and sets isScreenSharing to false` — start sharing, then `stopScreenShare()`; assert `replaceTrack` was called with `mockVideoTrack` (the camera track) and `isScreenSharing === false`.
    4. `SCRN-03: stopScreenShare stops the screen track` — assert `mockScreenTrack.stop` was called.
    5. `D-06: screenTrack.onended triggers stopScreenShare` — after start, invoke `mockScreenTrack.onended?.()` inside `act`, assert `isScreenSharing` returns to `false`.
    6. `D-08: teardown resets isScreenSharing to false` — start sharing, then `hangUp()`, assert `isScreenSharing === false` after teardown.
    7. `D-08: teardown stops the active screen track` — start sharing, `hangUp()`, assert `mockScreenTrack.stop` was called.
    8. `SCRN-01: startScreenShare shows "Screen sharing cancelled" toast on NotAllowedError` — override `getDisplayMedia` for one call via `vi.mocked(...).mockRejectedValueOnce(new DOMException('x','NotAllowedError'))`, assert a toast with message `Screen sharing cancelled` exists and `isScreenSharing` stays `false`.
    9. `SCRN-01: startScreenShare shows "Screen sharing unavailable" toast on non-NotAllowedError` — `mockRejectedValueOnce(new Error('boom'))`, assert toast `Screen sharing unavailable` and `isScreenSharing === false`.
    10. `startScreenShare is a no-op when there is no active peer connection` — call `startScreenShare()` WITHOUT starting a call first; assert `getDisplayMedia` was NOT called and `isScreenSharing` stays `false`.
    11. `startScreenShare shows an error and does not set isScreenSharing when no video sender exists` — override `getSenders` on the instance to return only an audio sender (`[{ track: { kind: 'audio' }, replaceTrack: vi.fn() }]`) before calling `startScreenShare`; assert `isScreenSharing` stays `false`.
    Reset `mockScreenTrack.stop` and `mockScreenTrack.onended` in a `beforeEach` (or at the top of each case) so cross-test state does not leak — follow the `mockAudioTrack.enabled = true` reset pattern already used at lines 545/566/616.
    These tests MUST fail at authoring time (the API does not exist yet) — that is expected RED state. Do NOT implement CallContext here.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `CallContext.test.tsx` imports `mockScreenTrack` from `./setup` (source assertion)
    - File contains an `it(...)` case referencing `startScreenShare` and asserting `getDisplayMedia` called with `{ video: true }` (source assertion: contains `startScreenShare` and `getDisplayMedia`)
    - File contains a case asserting `replaceTrack` called with `mockScreenTrack` (source assertion)
    - File contains a case asserting toast message `Screen sharing cancelled` and a case asserting `Screen sharing unavailable` (source assertion)
    - File contains a case invoking `mockScreenTrack.onended` and asserting `isScreenSharing` returns to false (source assertion: contains `onended`)
    - File contains a teardown case asserting `isScreenSharing` false after `hangUp()` (source assertion)
    - At least 11 new screen-share `it(...)` cases added (behavior assertion)
    - `npx tsc --noEmit` exits 0 (tests typecheck even while failing at runtime)
  </acceptance_criteria>
  <done>11 failing screen-share test cases exist and typecheck; they assert against the startScreenShare/stopScreenShare/isScreenSharing contract Plan 02 will implement.</done>
</task>

<task type="auto">
  <name>Task 3: Author failing CallPage Share-button and Camera-disabled-while-sharing test cases</name>
  <files>frontend/src/test/CallPage.test.tsx</files>
  <read_first>
    - frontend/src/test/CallPage.test.tsx (existing mock pattern — the `vi.mock('@/contexts/CallContext', ...)` block at lines 21-38 with mutable `mockIsMuted`/`mockIsCameraOff`/`mockIceState`; the `beforeEach` reset at lines 53-64; the button-by-role query pattern at lines 145-185)
    - frontend/src/pages/CallPage.tsx (current control bar — Mic/End/Camera buttons; Plan 03 adds the Share button and `isScreenSharing` consumption)
    - .planning/phases/06-screen-sharing/06-UI-SPEC.md (§5.2 Share button state table lines 162-174; Camera disabled-while-sharing row §5.2 lines 196-207; §8 copy lines 313-326)
  </read_first>
  <action>
    Extend `CallPage.test.tsx` to cover the Phase 6 UI. Add a mutable `let mockIsScreenSharing = false` and `const mockStartScreenShare = vi.fn()` / `const mockStopScreenShare = vi.fn()`, wire them into the `useCall()` mock object (add `isScreenSharing: mockIsScreenSharing`, `startScreenShare: mockStartScreenShare`, `stopScreenShare: mockStopScreenShare`), and reset them in `beforeEach` (set `mockIsScreenSharing = false`, `.mockClear()` the two fns). Write these failing cases:
    1. `SCRN-01: Share button has aria-label "Share screen" when not sharing` — `mockIsScreenSharing = false`; assert `screen.getByRole('button', { name: 'Share screen' })` is in document.
    2. `SCRN-01: Share button has aria-label "Stop sharing" when sharing` — `mockIsScreenSharing = true`; assert button named `Stop sharing` exists.
    3. `SCRN-01: clicking Share button (idle) calls startScreenShare` — `mockIsScreenSharing = false`; fireEvent.click the `Share screen` button; assert `mockStartScreenShare` called once.
    4. `SCRN-03: clicking Share button (active) calls stopScreenShare` — `mockIsScreenSharing = true`; click `Stop sharing` button; assert `mockStopScreenShare` called once.
    5. `D-05: Camera button is disabled when isScreenSharing is true` — set `mockIsScreenSharing = true` and `mockLocalStream` with one video track (`getVideoTracks: () => [{ enabled: true }]`); assert the camera button (aria-label `Camera disabled while sharing`) is in document and `.toBeDisabled()`.
    6. `D-03: Share button carries aria-pressed reflecting isScreenSharing` — assert `aria-pressed="true"` when sharing and `"false"` when idle (two assertions or two cases).
    These cases MUST fail at authoring time because the Share button does not yet exist in CallPage. Do NOT modify CallPage here.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `CallPage.test.tsx` declares `mockIsScreenSharing`, `mockStartScreenShare`, `mockStopScreenShare` and wires them into the `useCall()` mock (source assertion)
    - `beforeEach` resets `mockIsScreenSharing` and clears the two share fns (source assertion)
    - File contains a case querying `getByRole('button', { name: 'Share screen' })` and one for `'Stop sharing'` (source assertion)
    - File contains a case asserting `mockStartScreenShare` called once on idle click and `mockStopScreenShare` called once on active click (source assertion)
    - File contains a case querying the camera button by `'Camera disabled while sharing'` and asserting `.toBeDisabled()` (source assertion)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>6 failing CallPage screen-share UI test cases exist and typecheck against the Share button / disabled-camera contract Plan 03 will implement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → mocks | Tests run in jsdom against stubbed browser APIs; no real network, media, or peer connection crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — Phase 6 is frontend-only, zero new dependencies (06-RESEARCH.md "No New Packages Required"). No legitimacy checkpoint needed. |
| T-06-02 | Information Disclosure | test mocks | accept | Mocks contain no secrets; `getDisplayMedia`/`replaceTrack` stubs return synthetic objects only. Low risk. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (all test files and setup.ts typecheck)
- `cd frontend && npm run test -- --run` runs; the new screen-share cases appear as FAILING (RED) — expected because production code does not exist yet. Existing Phase 1-5 tests must remain GREEN.
</verification>

<success_criteria>
- setup.ts exposes getDisplayMedia, getSenders, replaceTrack mocks and exports mockScreenTrack
- 11 failing CallContext screen-share cases + 6 failing CallPage cases authored and typechecking
- No existing test regressed (Phase 1-5 suite still green)
- Zero new npm packages introduced
</success_criteria>

<output>
Create `.planning/phases/06-screen-sharing/06-01-SUMMARY.md` when done
</output>
