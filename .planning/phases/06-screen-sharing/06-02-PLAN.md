---
phase: 06-screen-sharing
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - frontend/src/contexts/CallContext.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-04]
must_haves:
  truths:
    - "Calling startScreenShare() captures the screen and the remote peer's video shows it without renegotiation (SCRN-01, SCRN-02, SCRN-04)"
    - "Calling stopScreenShare() restores the camera track on the sender and stops the screen capture (SCRN-03)"
    - "Clicking the browser native Stop-sharing bar restores the camera automatically via screenTrack.onended (D-06)"
    - "Ending the call while sharing stops the screen track and resets isScreenSharing (D-08)"
  artifacts:
    - path: "frontend/src/contexts/CallContext.tsx"
      provides: "isScreenSharing state, startScreenShare/stopScreenShare actions, screenTrackRef, teardown extension"
      contains: "startScreenShare"
      exports: ["CallProvider", "useCall"]
  key_links:
    - from: "frontend/src/contexts/CallContext.tsx startScreenShare"
      to: "RTCRtpSender.replaceTrack"
      via: "pcRef.current.getSenders().find(s => s.track?.kind === 'video')"
      pattern: "getSenders\\(\\).*replaceTrack"
    - from: "frontend/src/contexts/CallContext.tsx screenTrack.onended"
      to: "stopScreenShare"
      via: "stopScreenShareRef.current()"
      pattern: "onended.*stopScreenShareRef"
---

<objective>
Extend `CallContext` with screen-sharing state and actions: `isScreenSharing`, `startScreenShare`, `stopScreenShare`. Wire `getDisplayMedia` → `sender.replaceTrack(screenTrack)` so the remote peer sees the screen with no renegotiation (SCRN-02, SCRN-04), restore the camera on stop (SCRN-03), handle the browser native stop button (D-06), and extend `teardown()` to release the screen track (D-08).

Purpose: This is the behavioral core of Phase 6 — all four SCRN requirements are satisfied by the CallContext logic. The UI in Plan 03 is a thin consumer of this contract.
Output: Modified `CallContext.tsx` with new state, two `useCallback` actions, a `screenTrackRef`, a `stopScreenShareRef` (stale-closure guard), the extended `CallContextValue` interface, the Provider value additions, and the teardown extension. The Plan 01 RED tests for CallContext turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-screen-sharing/06-RESEARCH.md
@.planning/phases/06-screen-sharing/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isScreenSharing state, screenTrackRef, stopScreenShareRef, and extend CallContextValue interface</name>
  <files>frontend/src/contexts/CallContext.tsx</files>
  <behavior>
    - `isScreenSharing` initializes to `false` (existing Plan 01 teardown test expects false after hangUp)
    - The `CallContextValue` interface exposes `isScreenSharing: boolean`, `startScreenShare: () => Promise<void>`, `stopScreenShare: () => void`
    - `useCall()` consumers can read `isScreenSharing` and call both actions without TypeScript errors
  </behavior>
  <read_first>
    - frontend/src/contexts/CallContext.tsx (the `CallContextValue` interface at lines 26-41; the React state block at lines 79-86; the refs block at lines 89-95; the existing `isMuted`/`toggleMute` precedent at lines 84-85 and 142-151; the Provider value object at lines 505-526)
    - .planning/phases/06-screen-sharing/06-CONTEXT.md (D-07 interface additions)
    - .planning/phases/06-screen-sharing/06-RESEARCH.md (Pattern 4 screenTrackRef lines 232-248; Pattern 1 stale-closure ref lines 297-309)
  </read_first>
  <action>
    In `CallContext.tsx`:
    1. Extend the `CallContextValue` interface (currently ends at line 41 with `toggleCamera: () => void`) by adding exactly three members: `isScreenSharing: boolean`, `startScreenShare: () => Promise<void>`, `stopScreenShare: () => void`. Place them after `toggleCamera` for readability.
    2. Add React state: `const [isScreenSharing, setIsScreenSharing] = useState(false)` alongside the existing `isMuted`/`isCameraOff` state declarations (after line 85).
    3. Add a ref `const screenTrackRef = useRef<MediaStreamTrack | null>(null)` alongside the existing `localStreamRef`/`pcRef` refs (after line 90).
    4. Add a mutable callback ref `const stopScreenShareRef = useRef<() => void>(() => {})` — this mirrors the established `handleSignalRef` pattern (lines 361-364) to prevent the stale-closure bug in `screenTrack.onended` (RESEARCH Pitfall 1). It will be populated in Task 2 via a `useEffect`.
    Do NOT add the Provider value members yet (done in Task 2 after the actions exist) — but DO leave the interface complete so the file still typechecks against existing consumers once the value object is updated in Task 2.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `CallContextValue` interface contains `isScreenSharing: boolean`, `startScreenShare: () => Promise<void>`, `stopScreenShare: () => void` (source assertion)
    - File contains `const [isScreenSharing, setIsScreenSharing] = useState(false)` (source assertion)
    - File contains `const screenTrackRef = useRef<MediaStreamTrack | null>(null)` (source assertion)
    - File contains `const stopScreenShareRef = useRef` (source assertion)
    - `npx tsc --noEmit` exits 0 (interface additions plus value object completed in Task 2 keep the file consistent — run this verify after Task 2 if the value object is the blocker)
  </acceptance_criteria>
  <done>State, refs, and interface members for screen sharing are declared following the isMuted/handleSignalRef precedents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement startScreenShare, stopScreenShare, teardown extension, and wire into Provider value</name>
  <files>frontend/src/contexts/CallContext.tsx</files>
  <behavior>
    - `startScreenShare()` with an active pc: calls `getDisplayMedia({ video: true })`, finds the video sender via `getSenders().find(s => s.track?.kind === 'video')`, calls `sender.replaceTrack(screenTrack)`, sets `isScreenSharing` true, sets `screenTrackRef.current`, attaches `screenTrack.onended` → `stopScreenShareRef.current()` (Plan 01 SCRN-01/02 tests pass)
    - `startScreenShare()` with NO pc: returns early, does not call getDisplayMedia, isScreenSharing stays false (Plan 01 no-pc test passes)
    - `startScreenShare()` when getDisplayMedia rejects with NotAllowedError: shows "Screen sharing cancelled" toast; any other error: "Screen sharing unavailable" toast; isScreenSharing stays false (Plan 01 toast tests pass)
    - `startScreenShare()` when no video sender found (audio-only call): stops screen track, isScreenSharing stays false (Plan 01 no-sender test passes)
    - `stopScreenShare()`: replaces sender track with camera track from `localStreamRef.current.getVideoTracks()[0]`, stops screen track, nulls screenTrackRef, sets isScreenSharing false (Plan 01 SCRN-03 tests pass)
    - `screenTrack.onended` invocation triggers stopScreenShare via the ref (Plan 01 D-06 test passes)
    - `teardown()` stops the active screen track, nulls screenTrackRef, sets isScreenSharing false (Plan 01 D-08 tests pass)
  </behavior>
  <read_first>
    - frontend/src/contexts/CallContext.tsx (the `toggleCamera` useCallback at lines 158-166 as the action style precedent; the `teardown` useCallback at lines 176-208 — note the numbered step sequence and where step 2 "Stop media tracks" ends at line 189; the `handleSignalRef` mirror useEffect at lines 361-364; the `addToast` helper at lines 107-113; the Provider value object at lines 505-526)
    - .planning/phases/06-screen-sharing/06-RESEARCH.md (Code Examples: complete startScreenShare lines 360-401, complete stopScreenShare lines 406-419, teardown extension lines 422-430; Pitfall 2 pc-null-after-await guard lines 311-326; Pitfall 5 no-video-sender guard lines 346-352)
    - .planning/phases/06-screen-sharing/06-UI-SPEC.md (§8 error toast strings + styles lines 328-339: "Screen sharing cancelled" → `bg-slate-800 border border-slate-700 text-slate-400`; "Screen sharing unavailable" → `bg-slate-800 border border-amber-600/40 text-amber-400`)
  </read_first>
  <action>
    Implement the two actions in `CallContext.tsx`, defining `stopScreenShare` BEFORE `startScreenShare` so the onended wiring is clear (both are `useCallback`):

    `stopScreenShare = useCallback((): void => {...}, [])` per SCRN-03 / D-06:
    - Read `cameraTrack = localStreamRef.current?.getVideoTracks()[0]`
    - Read `videoSender = pcRef.current?.getSenders().find(s => s.track?.kind === 'video')` (Option A from RESEARCH Pattern 5 — re-query, no stored sender ref)
    - If both `videoSender` and `cameraTrack` exist: `void videoSender.replaceTrack(cameraTrack)`
    - `screenTrackRef.current?.stop()`, then `screenTrackRef.current = null`, then `setIsScreenSharing(false)`

    `startScreenShare = useCallback(async (): Promise<void> => {...}, [addToast])` per SCRN-01/02/04, D-06:
    - Guard: `if (!pcRef.current) return` (RESEARCH anti-pattern — never call when no pc)
    - `try { const screenStream = await navigator.mediaDevices.getDisplayMedia({ video: true }); screenTrack = screenStream.getVideoTracks()[0] }`
    - `catch (err)`: if `err instanceof Error && err.name === 'NotAllowedError'` → `addToast('Screen sharing cancelled', 'bg-slate-800 border border-slate-700 text-slate-400')`; else → `addToast('Screen sharing unavailable', 'bg-slate-800 border border-amber-600/40 text-amber-400')`; then `return` (isScreenSharing stays false)
    - After await resolves, re-check `if (!pcRef.current) { screenTrack.stop(); return }` (Pitfall 2 — call may have ended during picker)
    - `videoSender = pcRef.current.getSenders().find(s => s.track?.kind === 'video')`; if `!videoSender` → `screenTrack.stop(); return` (Pitfall 5 — audio-only call has no video sender)
    - `await videoSender.replaceTrack(screenTrack)`
    - `screenTrackRef.current = screenTrack`; `setIsScreenSharing(true)`
    - `screenTrack.onended = () => stopScreenShareRef.current()` (use the ref, NOT a direct closure — RESEARCH Pitfall 1)

    Add the `stopScreenShareRef` mirror effect right after `stopScreenShare` is defined: `useEffect(() => { stopScreenShareRef.current = stopScreenShare }, [stopScreenShare])`.

    Extend `teardown()` (D-08): after step 2 "Stop media tracks" (immediately after line 189 `localStreamRef.current = null`), add a step "2.5 — Phase 6: stop screen track if active": `screenTrackRef.current?.stop(); screenTrackRef.current = null`. Also add `setIsScreenSharing(false)` to the step-5 state-reset block alongside the existing `setIsMuted(false)` / `setIsCameraOff(false)` calls (near lines 205-207).

    Finally add `isScreenSharing`, `startScreenShare`, `stopScreenShare` to the Provider value object (lines 505-526) alongside `isMuted`/`toggleMute`/`toggleCamera`.

    Do NOT modify `localStream` / `localStreamRef` / `setLocalStream` in either action — the local PiP must keep showing the camera (D-04 anti-pattern: never call `setLocalStream(screenStream)`). Do NOT use `addTrack()` — only `replaceTrack()` (SCRN-02 forbids renegotiation).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run CallContext</automated>
  </verify>
  <acceptance_criteria>
    - `startScreenShare` calls `navigator.mediaDevices.getDisplayMedia({ video: true })` (behavior: Plan 01 test "startScreenShare calls getDisplayMedia" passes)
    - `startScreenShare` calls `videoSender.replaceTrack(screenTrack)` and sets `isScreenSharing` true (behavior: Plan 01 SCRN-02 test passes)
    - `startScreenShare` returns early when `pcRef.current` is null and does NOT call getDisplayMedia (behavior: Plan 01 no-pc test passes)
    - NotAllowedError produces "Screen sharing cancelled" toast; other errors produce "Screen sharing unavailable"; isScreenSharing stays false (behavior: Plan 01 toast tests pass)
    - `stopScreenShare` calls `replaceTrack` with the camera track, stops the screen track, sets isScreenSharing false (behavior: Plan 01 SCRN-03 tests pass)
    - `screenTrack.onended = () => stopScreenShareRef.current()` present (source assertion: file contains `onended` and `stopScreenShareRef.current()`)
    - `teardown` contains `screenTrackRef.current?.stop()` and `setIsScreenSharing(false)` (source assertion)
    - File does NOT contain `setLocalStream(screenStream)` and does NOT call `addTrack` inside startScreenShare/stopScreenShare (source assertion — D-04/SCRN-02 anti-patterns absent)
    - `npm run test -- --run CallContext` exits 0 (all 11 Plan 01 screen-share cases + all prior CallContext cases green)
  </acceptance_criteria>
  <done>startScreenShare/stopScreenShare implemented per research, onended wired via ref, teardown extended, Provider value exposes the three new members; full CallContext test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser screen picker → app | `getDisplayMedia` returns a user-selected screen track into the app; the user controls what is captured at the OS level |
| Local app → remote peer (WebRTC) | The replaced video track flows over the already-authenticated, DTLS/SRTP-encrypted peer connection established in Phase 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | screen content → remote peer | accept | Screen share occurs only on an authenticated, established WebRTC session (Phase 1 JWT + Phase 4 connection). No new trust boundary; user explicitly selects what to share via the OS picker. (06-RESEARCH.md Security Domain) |
| T-06-04 | Denial of Service (resource) | screen capture session after call ends | mitigate | `teardown()` step 2.5 calls `screenTrackRef.current?.stop()` to release the OS capture session (D-08). `stopScreenShare()` also stops the track, removing the browser "Stop sharing" overlay. |
| T-06-05 | Tampering | null pcRef dereference after async getDisplayMedia | mitigate | Re-check `if (!pcRef.current)` after `await getDisplayMedia()` returns and stop the orphaned screen track before returning (RESEARCH Pitfall 2). Prevents crash if call ends during picker. |
| T-06-06 | Tampering | npm installs | accept | No package installs — Phase 6 adds zero dependencies (06-RESEARCH.md). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run test -- --run CallContext` exits 0 — all Plan 01 screen-share RED cases now GREEN, no prior CallContext case regressed
- `cd frontend && npx tsc --noEmit` exits 0
- Source check: `localStream`/`setLocalStream` untouched in screen-share paths (D-04); only `replaceTrack` used, never `addTrack` (SCRN-02)
</verification>

<success_criteria>
- CallContext exposes isScreenSharing, startScreenShare, stopScreenShare
- replaceTrack swaps camera↔screen with no renegotiation (SCRN-02); remote update is automatic via existing ontrack binding (SCRN-04)
- Browser native stop handled via screenTrack.onended → stopScreenShareRef (D-06)
- teardown releases screen track and resets isScreenSharing (D-08)
- Full CallContext test suite green; zero new packages
</success_criteria>

<output>
Create `.planning/phases/06-screen-sharing/06-02-SUMMARY.md` when done
</output>
